# 赛题5：业务数据准确性验证工具产品需求文档

## 1. 背景与痛点

研发测试提效率赛道要求通过 AI 与工程工具提升测试效率和质量。赛题5聚焦业务数据准确性保障，要求工具能够基于业务规则定义对测试数据、准生产数据或演示数据进行核验，输出差错信息、差错原因和修复建议。

当前赛题输入材料包含订单、订单明细、商品、支付、库存流水 5 张业务表，以及业务规则库、字段约束说明、关联逻辑说明和校验场景覆盖矩阵。业务数据准确性问题的核心不在于数据能否读取，而在于数据是否符合业务事实、跨表链路和指标口径。

主要痛点如下：

1. **业务规则分散，人工执行成本高**：规则散落在文档、SQL、测试经验和人工核查步骤中，缺少统一执行入口。
2. **跨表异常定位复杂**：订单金额、明细小计、支付金额、库存扣减等关系需要跨多张表聚合和对比，人工核查容易遗漏。
3. **异常证据链不足**：只给出异常字段无法说明为什么异常，测试人员仍需继续追查关联记录和计算过程。
4. **规则结果难以复现**：人工核查过程依赖个人经验，难以保证同一输入多次得到一致结果。
5. **修复建议依赖业务理解**：开发或测试人员不仅需要知道哪条数据异常，还需要理解影响范围、核查路径和修复方向。

## 2. 目标与预期收益

### 2.1 产品目标

建设一款面向赛题5交付、演示答辩和后续维护的业务数据准确性验证工具。工具通过规则库驱动数据核验，自动发现单表字段异常、单表业务规则异常、多表关联异常和指标一致性异常，并输出可追溯、可解释、可复现的异常疑点清单与核验报告。

当前版本定位为 MVP 演示工具，不建设完整数据质量平台。产品以 Web 工作台为主界面，以 REST API 和 `curl` 命令作为 CLI 核验口径。

### 2.2 预期收益

1. **提升核验效率**：将 30 条业务规则固化为可重复执行的校验流程，减少人工跨表查询和手工对账。
2. **提升异常可信度**：每条异常保留规则编号、异常表、记录主键、实际值、期望值和证据链。
3. **提升演示可复现性**：同一 Excel 规则资产和 H2 seed 业务数据可重复生成一致的异常统计和报告。
4. **提升问题解释能力**：通过本地规则化逻辑或 AI 辅助生成原因、影响、建议和只读 SQL 草案。
5. **沉淀可扩展规则资产**：通过规则模板、模板参数校验和推荐能力，为后续减少硬编码规则提供基础。

## 3. 目标受众与使用场景

### 3.1 目标受众

| 角色 | 关注点 | 主要操作 |
|---|---|---|
| 测试人员 | 快速发现测试数据中的业务错误 | 上传 Excel、执行校验、筛选异常、查看详情、导出报告 |
| 测试负责人 | 评估规则覆盖和数据质量风险 | 查看异常总览、严重等级分布、规则命中情况 |
| 业务分析人员 | 理解异常原因和业务影响 | 查看规则说明、关联证据、原因分析和影响说明 |
| 开发人员 | 修复数据或调整造数逻辑 | 查看异常证据、人工核查 SQL 草案和修复建议 |
| 比赛评委 | 判断工具完整度与可复现性 | 查看演示页面、接口命令、异常截图和导出报告 |

### 3.2 核心使用场景

1. 用户启动后端与前端，打开业务数据准确性验证工作台。
2. 用户上传赛题5 Excel 输入案例，系统解析规则资产、字段约束、关联逻辑和场景矩阵。
3. 系统从 H2 seed 业务表读取 5 张业务表，形成标准化 `DataTable` / `DataRow`。
4. 用户查看规则模板化工作台，可对当前规则或可见规则批量生成 AI/本地模板推荐，并人工应用推荐。
5. 用户点击开始校验，系统执行 R001 到 R030 规则，按模板执行器优先、内置规则兜底的策略生成异常。
6. 用户查看汇总指标、异常疑点清单、异常详情和证据链，并按严重等级、业务表、规则编号筛选。
7. 用户按需生成 AI 辅助分析、只读校验 SQL 草案或人工核查 SQL 草案。
8. 用户导出 Markdown 或 HTML 报告，用于答辩展示、问题跟踪和后续修复。

## 4. 功能性需求

### 4.1 核心功能列表

| 编号 | 功能 | 当前要求 |
|---|---|---|
| F001 | Excel 上传与规则资产解析 | 支持上传 `.xlsx`，校验必需 sheet，解析 30 条规则、15 个校验场景和 7 条关联逻辑 |
| F002 | 演示业务数据读取 | 通过 Flyway seed 在 H2 初始化 5 张业务表，并由 `BusinessTableDataProvider` 读取 |
| F003 | 数据集与快照保存 | 保存数据集、业务表元数据、规则定义、规则绑定、校验任务、异常和证据 |
| F004 | 规则执行 | 支持 R001 到 R030，覆盖字段约束、单表业务规则、多表关联核对和指标一致性校验 |
| F005 | 规则模板化工作台 | 展示规则绑定、模板参数、模板能力矩阵，支持推荐当前规则和批量推荐可见规则 |
| F006 | 模板执行与内置兜底 | 规则绑定为 `TEMPLATE` 时调用模板执行器，否则使用内置规则逻辑 |
| F007 | 模板参数校验 | 校验模板编码、表名、字段名、参数结构、表达式、谓词、聚合和 join key |
| F008 | 异常汇总 | 输出规则总数、已执行规则数、异常总数、严重异常、警告异常和耗时 |
| F009 | 异常列表与筛选 | 按 `jobId` 查询异常，可按严重等级、业务表和规则编号筛选 |
| F010 | 异常详情与证据链 | 展示规则、记录、实际值、期望值、原因、影响、建议和关联证据 |
| F011 | AI 辅助异常分析 | 基于异常和证据生成原因、影响、建议和证据摘要；AI 不可用时本地降级 |
| F012 | 只读 SQL 草案 | 基于 `findingId` 生成 `VALIDATION_CHECK` 或 `MANUAL_REVIEW` SQL 草案，仅展示不执行 |
| F013 | 规则模板推荐 | 根据规则定义和字段快照推荐模板绑定，推荐结果必须经校验并由用户手动应用 |
| F014 | 报告导出 | 支持生成 Markdown 报告；`format=HTML` 时生成 HTML 文件；其他格式按 Markdown 内容输出 |
| F015 | 预留数据源接口 | `/api/datasources/database-table` 和 `/api/datasources/sql-query` 当前返回 P3 预留提示 |

当前支持 13 类规则模板：

| 模板 | 用途 |
|---|---|
| `NOT_NULL` | 字段非空 |
| `NON_NEGATIVE` | 数值非负 |
| `NUMERIC_TYPE` | 数值类型 |
| `FIELD_EXPRESSION` | 单表字段表达式 |
| `ROW_EXPRESSION` | 结构化行表达式 |
| `EXISTS_IN_TABLE` | 简单跨表存在性 |
| `RELATION_EXISTS` | 结构化关系存在性 |
| `FIELD_EQUALS` | 旧版跨表字段一致 |
| `JOIN_ASSERT` | 结构化跨表 join 断言 |
| `AGGREGATION_EQUALS` | 旧版聚合字段一致 |
| `AGGREGATE_ASSERT` | 结构化聚合断言 |
| `DUPLICATE_ASSERT` | 结构化分组次数断言 |
| `DUPLICATE_CHECK` | 旧版重复记录检查 |

当前明确边界：

1. 数据库表输入和 SQL 查询输入为 P3 预留能力，当前 MVP 仍使用 Excel 上传入口和 H2 seed 业务表。
2. Excel 报告属于原设计目标，当前代码未落地；当前报告主要为 Markdown 和 HTML。
3. AI 只做解释、推荐和 SQL 草案，不参与最终确定性校验，不自动修改业务数据，不自动执行 SQL。
4. 未建设权限系统、审计系统、多租户、任务调度和大规模异步执行能力。

### 4.2 交互设计（CLI 输入输出定义）

CLI 采用现有 REST API 的 `curl` 调用方式定义。命令默认后端地址为：

```text
http://localhost:8080
```

#### 4.2.1 上传 Excel

输入：

```bash
curl -F "file=@../各赛题输入案例/赛题5-业务数据准确性验证工具-输入案例.xlsx" \
  http://localhost:8080/api/files/upload
```

输出字段：

| 字段 | 说明 |
|---|---|
| `datasetId` | 数据集编号，后续校验和规则查询使用 |
| `fileName` | 上传文件名 |
| `businessTableCount` | 业务表数量，预期为 5 |
| `ruleCount` | 规则数量，预期为 30 |
| `scenarioCount` | 场景数量，预期为 15 |
| `relationCount` | 关联逻辑数量，预期为 7 |

示例输出：

```json
{
  "success": true,
  "data": {
    "datasetId": "ds_xxx",
    "fileName": "赛题5-业务数据准确性验证工具-输入案例.xlsx",
    "businessTableCount": 5,
    "ruleCount": 30,
    "scenarioCount": 15,
    "relationCount": 7
  }
}
```

#### 4.2.2 查询规则与模板绑定

输入：

```bash
curl "http://localhost:8080/api/rules?datasetId=ds_xxx"
```

输出字段：

| 字段 | 说明 |
|---|---|
| `ruleId` | 规则编号 |
| `ruleName` | 规则名称 |
| `category` | 规则分类 |
| `severity` | 严重等级 |
| `executorType` | `BUILTIN` 或 `TEMPLATE` |
| `templateCode` | 绑定模板编码，可为空 |
| `templateParams` | 模板参数 JSON |

#### 4.2.3 启动校验

输入：

```bash
curl -H "Content-Type: application/json" \
  -d '{"datasetId":"ds_xxx","enableAiAnalysis":false}' \
  http://localhost:8080/api/validations
```

输出字段：

| 字段 | 说明 |
|---|---|
| `jobId` | 校验任务编号，后续查询汇总和异常使用 |
| `datasetId` | 被校验的数据集编号 |
| `status` | 任务状态 |
| `findingCount` | 异常数量 |

#### 4.2.4 查询校验汇总

输入：

```bash
curl http://localhost:8080/api/validations/job_xxx/summary
```

输出字段：

| 字段 | 说明 |
|---|---|
| `jobId` | 校验任务编号 |
| `totalRules` | 总规则数 |
| `executedRules` | 已执行规则数 |
| `findingCount` | 异常总数 |
| `criticalCount` | 严重异常数 |
| `warningCount` | 警告异常数 |
| `durationMillis` | 校验耗时 |

#### 4.2.5 查询异常列表

输入：

```bash
curl "http://localhost:8080/api/findings?jobId=job_xxx"
```

按业务表筛选：

```bash
curl "http://localhost:8080/api/findings?jobId=job_xxx&tableName=t_order"
```

按严重等级和规则筛选：

```bash
curl "http://localhost:8080/api/findings?jobId=job_xxx&severity=CRITICAL&ruleId=R006"
```

输出字段：

| 字段 | 说明 |
|---|---|
| `total` | 当前查询条件下的异常数量 |
| `items` | 异常列表 |
| `findingId` | 异常编号 |
| `ruleId` | 命中规则编号 |
| `severity` | 严重等级 |
| `tableName` | 异常主表 |
| `recordKey` | 异常记录主键 |
| `description` | 异常描述 |

#### 4.2.6 查询异常详情与证据链

输入：

```bash
curl http://localhost:8080/api/findings/finding_xxx
```

输出字段：

| 字段 | 说明 |
|---|---|
| `finding` | 异常主信息 |
| `evidences` | 证据明细 |
| `actualValue` | 实际值 |
| `expectedValue` | 期望值 |
| `reason` | 原因解释 |
| `impact` | 影响说明 |
| `suggestion` | 修复建议 |

#### 4.2.7 生成 AI 或本地降级异常分析

输入：

```bash
curl http://localhost:8080/api/ai/findings/finding_xxx/analysis
```

输出字段：

| 字段 | 说明 |
|---|---|
| `source` | `OPENAI_COMPATIBLE` 或 `LOCAL_RULE_BASED` |
| `generatedByAi` | 是否由模型生成 |
| `reason` | 原因分析 |
| `impact` | 影响说明 |
| `suggestion` | 修复建议 |
| `evidenceSummary` | 证据摘要 |
| `warnings` | 降级或风险提示 |

#### 4.2.8 生成只读校验 SQL 草案

输入：

```bash
curl -H "Content-Type: application/json" \
  -d '{"findingId":"finding_xxx","draftType":"VALIDATION_CHECK"}' \
  http://localhost:8080/api/ai/sql-drafts
```

输出字段：

| 字段 | 说明 |
|---|---|
| `draftType` | `VALIDATION_CHECK` |
| `sql` | 用于复现当前规则命中的只读 SQL 草案 |
| `source` | 模型生成或本地降级来源 |
| `generatedByAi` | 是否由模型生成 |
| `warnings` | 安全校验或降级提示 |

#### 4.2.9 生成人工核查 SQL 草案

输入：

```bash
curl -H "Content-Type: application/json" \
  -d '{"findingId":"finding_xxx","draftType":"MANUAL_REVIEW"}' \
  http://localhost:8080/api/ai/sql-drafts
```

输出说明：

`MANUAL_REVIEW` 面向人工排查，通常按主键定位当前异常记录，并投影规则相关字段和证据字段。系统只返回 SQL 文本，不自动执行。

#### 4.2.10 生成与下载报告

生成 Markdown 报告：

```bash
curl -H "Content-Type: application/json" \
  -d '{"jobId":"job_xxx","format":"MARKDOWN"}' \
  http://localhost:8080/api/reports
```

生成 HTML 报告：

```bash
curl -H "Content-Type: application/json" \
  -d '{"jobId":"job_xxx","format":"HTML"}' \
  http://localhost:8080/api/reports
```

下载报告：

```bash
curl -O http://localhost:8080/api/reports/rpt_xxx/download
```

输出字段：

| 字段 | 说明 |
|---|---|
| `reportId` | 报告编号 |
| `format` | 报告格式 |
| `filePath` | 后端保存路径 |

#### 4.2.11 P3 预留数据源接口

当前以下接口只返回预留能力提示：

```bash
curl -X POST http://localhost:8080/api/datasources/database-table
curl -X POST http://localhost:8080/api/datasources/sql-query
```

预期返回含义：

```text
数据库表输入为P3预留能力，当前MVP请使用Excel上传
SQL查询结果输入为P3预留能力，当前MVP请使用Excel上传
```

## 5. 非功能性需求

### 5.1 可运行性

工具应支持本地独立运行。后端使用 Java 11、Maven、Spring Boot；前端使用 Node.js、npm、Vue 3、Vite。默认后端端口为 `8080`，前端开发端口为 `5173`。

### 5.2 可复现性

同一赛题 Excel、同一 H2 seed 数据、同一规则绑定和同一配置下，多次执行应得到一致的异常清单、汇总统计和报告内容。AI 生成内容不应影响确定性规则判定。

### 5.3 可核验性

每条异常必须能追溯到规则编号、规则名称、异常表、记录主键、字段名、实际值、期望值、场景编号和关联证据。跨表异常必须保留关联记录、聚合值或计算过程。

### 5.4 易用性

测试人员应能通过页面完成上传、校验、筛选、查看详情、生成分析和导出报告。CLI/curl 路径应能不依赖前端完成同样的核心链路核验。

### 5.5 性能要求

赛题5输入规模较小，当前版本以准确性、解释性和演示稳定性优先。上传、校验和报告生成应在本地演示环境中短时间完成，并记录校验耗时。

### 5.6 安全要求

1. 不硬编码真实生产密钥、凭证或 API Key。
2. AI 输出只作为建议，不作为自动判定依据。
3. SQL 草案只允许单条只读 `SELECT`，禁止写操作、结构变更、多语句、注释和危险关键字。
4. 模板推荐结果必须经过后端参数校验，不能绕过模板白名单、字段白名单和表达式校验。
5. 生产化业务库接入需使用只读账号和独立数据源，当前版本不直接连接生产数据库。

### 5.7 可维护性

规则执行应保持“模板优先、内置兜底”的结构。新增规则优先沉淀为模板参数或结构化 DSL；只有复杂业务逻辑确实无法模板化时，才新增内置规则分支。

## 6. 架构与依赖方案

### 6.1 逻辑架构

```text
Vue 3 前端工作台
  |
  v
Spring Boot REST API
  |
  v
应用服务层
  ExcelImportService / ValidationService / RuleBindingService
  AiAssistService / ReportService
  |
  v
规则执行层
  TemplateRuleExecutor / 内置 R001-R030 规则逻辑
  RowExpressionEvaluator / TemplateExpressionEvaluator
  |
  v
数据访问层
  Spring Data JPA Repository / JdbcTemplate / Flyway
  |
  v
H2 演示系统库 + H2 演示业务表
```

### 6.2 端到端数据流

```text
上传赛题 Excel
  -> 校验必需 sheet 并解析规则资产
  -> 从 H2 seed 业务表读取标准化业务数据
  -> 保存 dataset、table snapshot、rule definition、rule binding
  -> 用户查看或调整规则模板绑定
  -> 启动规则校验
  -> 保存 validation job、finding、evidence
  -> 查询汇总、异常列表和异常详情
  -> 生成 AI 分析、SQL 草案或核验报告
```

### 6.3 关键模块

| 模块 | 当前实现 |
|---|---|
| 前端工作台 | `frontend/src/App.vue`、`frontend/src/api/client.js`、`frontend/src/styles/app.css` |
| 文件上传 | `UploadController`、`ExcelImportService` |
| 业务表读取 | `BusinessTableDataProvider`、Flyway H2 seed |
| 规则校验 | `ValidationService`、`TemplateRuleExecutor`、内置 R001-R030 分支 |
| 模板绑定 | `RuleBindingService`、`TemplateBindingValidator` |
| AI 辅助 | `AiAssistService`、`OpenAiCompatibleChatClient` |
| SQL 草案 | `AiAssistService`、`SqlIdentifierResolver` |
| 报告导出 | `ReportController`、`ReportService` |
| 持久化 | Spring Data JPA Repository、Flyway schema |

### 6.4 技术依赖

| 层次 | 依赖 |
|---|---|
| 后端语言与框架 | Java 11、Spring Boot 2.7.18、Spring MVC |
| 持久化 | Spring Data JPA、H2、Flyway、预留 MySQL profile |
| 数据读取 | Apache POI、JdbcTemplate |
| JSON | Jackson |
| 前端 | Vue 3、Vite |
| AI 接入 | OpenAI-compatible chat completions，默认关闭 |
| 测试 | JUnit 5、Spring Boot Test、AssertJ |

### 6.5 配置方案

默认配置：

```yaml
server:
  port: 8080

spring:
  profiles:
    active: h2

app:
  storage:
    upload-dir: data/uploads
    report-dir: data/reports
  validation:
    enable-ai-default: false
    stop-on-rule-error: false
  ai:
    enabled: ${LOCAL_AI_ENABLED:false}
    provider: openai-compatible
    endpoint: ${LOCAL_AI_ENDPOINT:}
    api-key: ${LOCAL_AI_API_KEY:}
    model: ${LOCAL_AI_MODEL:local-model}
```

## 7. 分发与维护策略

### 7.1 本地开发与演示分发

推荐以完整仓库目录分发，包含：

```text
backend/
frontend/
各赛题输入案例/
赛题5-业务数据准确性验证工具需求文档.md
赛题5-业务数据准确性验证工具总技术文档.md
赛题5-业务数据准确性验证工具产品需求文档.md
赛题5-业务数据准确性验证工具部署演示操作文档.md
```

后端启动：

```bash
cd backend
mvn spring-boot:run
```

前端启动：

```bash
cd frontend
npm install
npm run dev
```

浏览器访问：

```text
http://localhost:5173
```

### 7.2 Jar 分发

后端可打包为 Jar。CLI 分发要求 JRE 11 或更高版本：

```bash
cd backend
mvn clean package
java -jar target/data-validator-0.1.0.jar --version
```

仓库根目录提供轻量启动脚本：

```bash
bin/data-validator --version
bin/data-validator lint --config examples/distribution-minimal/validator.yml
bin/data-validator run --config examples/distribution-minimal/validator.yml
```

脚本默认查找 `backend/target/data-validator-0.1.0.jar`。如发布包使用其他 Jar 位置，可通过环境变量覆盖：

```bash
DATA_VALIDATOR_JAR=/opt/data-validator/data-validator-0.1.0.jar bin/data-validator --version
JAVA_OPTS="-Xmx512m" bin/data-validator run --config examples/distribution-minimal/validator.yml
```

推荐发布包目录：

```text
bin/data-validator
backend/target/data-validator-0.1.0.jar
examples/distribution-minimal/
```

`examples/distribution-minimal/` 只包含配置、规则、样例数据和预期结果说明，不包含生成报告、临时文件、密钥或本地环境路径。

如需单服务演示，可将前端构建产物放入后端静态资源目录后重新打包；当前推荐方式仍为前后端分别启动，便于排查问题。

### 7.3 AI 能力分发

AI 能力默认为关闭。启用时通过环境变量配置 OpenAI-compatible 服务：

```bash
LOCAL_AI_ENABLED=true \
LOCAL_AI_ENDPOINT=http://127.0.0.1:8317/v1/chat/completions \
LOCAL_AI_API_KEY=123456 \
LOCAL_AI_MODEL=gpt-5.4 \
mvn spring-boot:run
```

要求：

1. API Key 不写入源码。
2. 模型不可用时自动降级到本地规则化结果。
3. 模型生成的 SQL 必须通过只读、单语句和白名单校验。

### 7.4 报告与数据维护

上传文件和报告默认保存在后端目录：

```text
backend/data/uploads/
backend/data/reports/
```

H2 默认使用内存库，重启后运行数据会清空，适合比赛演示。MySQL profile 可用于后续持久化演示数据，但生产业务库接入仍属于后续演进。

### 7.5 规则与版本维护

1. 规则定义继续以赛题 Excel 的 `业务规则库` 和 `校验场景覆盖矩阵` 为输入资产。
2. 规则绑定通过 `rule_binding` 保存，支持 `BUILTIN` 与 `TEMPLATE` 切换。
3. 新增规则优先复用 `ROW_EXPRESSION`、`RELATION_EXISTS`、`JOIN_ASSERT`、`AGGREGATE_ASSERT`、`DUPLICATE_ASSERT` 等结构化模板。
4. 每次扩展模板能力需同步补充模板参数校验和单元测试。

### 7.6 后续演进

1. 接入真实业务只读库，完善 `/api/datasources/database-table` 与 `/api/datasources/sql-query`。
2. 将更多默认规则绑定迁移到结构化模板，降低 R001 到 R030 的特例分支。
3. 扩展 Excel 报告，按汇总、异常明细、规则覆盖和证据链分 sheet 输出。
4. 增加规则版本、绑定版本和报告可复现元数据。
5. 增强前端模板参数差异展示、批量应用确认和失败原因说明。

## 附录：验收检查清单

| 检查项 | 验收标准 |
|---|---|
| 文档一致性 | 不把 P3 预留的数据源能力写成已实现能力 |
| 功能覆盖 | 覆盖 5 张业务表、30 条规则、15 个场景、7 条关联逻辑、13 类模板 |
| CLI 可复现 | curl 示例可串联上传、校验、汇总、异常、详情、报告链路 |
| 安全边界 | 明确 AI 降级、SQL 只读、不自动执行、不修改业务数据 |
| 报告边界 | 明确当前主要支持 Markdown/HTML，Excel 报告为后续演进 |
| 分发边界 | 明确 H2 默认演示、MySQL profile 预留、前后端分离启动方式 |
