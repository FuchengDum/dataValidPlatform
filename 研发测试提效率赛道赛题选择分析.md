# 研发测试提效率赛道赛题选择分析

## 结论

结合新增的各赛题输入案例，仍然建议首选 **赛题5：业务数据准确性验证工具**。

新增案例反而强化了这个结论：赛题5输入最完整，已经包含订单、订单明细、商品、支付、库存流水等业务表，配套提供业务规则库、字段约束说明、关联逻辑说明和校验场景覆盖矩阵。它最容易形成“输入数据 -> 规则解析 -> 异常检出 -> 原因解释 -> 修复建议 -> 报告/界面展示”的完整闭环。

如果目标是 **稳妥完赛并有较好展示效果**，推荐优先级调整为：

1. 赛题5：业务数据准确性验证工具
2. 赛题2：通用数据加载入库工具
3. 赛题3：保留数据关系和逻辑的测试数据集构造
4. 赛题4：支持敏感信息脱敏的通用工具
5. 赛题1：根据工程代码补全、完善需求描述和文档

如果目标是 **更偏创新冲奖**，赛题3仍可作为第二选择，但实现风险明显高于赛题2。

## 基于输入案例的重新评分

| 赛题 | 输入案例完整度 | 可实现闭环 | AI 增益空间 | 演示效果 | 实现风险 | 推荐度 | 结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| 赛题1 文档补全 | 2.5 | 3 | 3 | 2.5 | 2 | 2.5 | 案例偏示意，差异化弱 |
| 赛题2 数据加载 | 4.5 | 4.5 | 2.5 | 4 | 2.5 | 4 | 最适合稳妥完赛 |
| 赛题3 测试数据构造 | 4 | 3.5 | 4.5 | 4 | 4 | 4 | 创新强，但工程风险高 |
| 赛题4 脱敏工具 | 3 | 4.5 | 2.5 | 3.5 | 1.5 | 3.5 | 最容易做完整，但亮点有限 |
| 赛题5 数据准确性验证 | 5 | 5 | 4.5 | 5 | 3 | 5 | 首选，价值、输入、展示都最强 |

## 新增输入案例带来的关键变化

### 赛题5：首选进一步确认

赛题5的输入案例质量最高，文件 `赛题5-业务数据准确性验证工具-输入案例.xlsx` 包含：

- 业务数据表：`订单表_t_order`、`订单明细表_t_order_item`、`商品表_t_product`、`支付表_t_payment`、`库存流水表_t_inventory_log`
- 规则资产：`业务规则库`，包含 R001 到 R030 共 30 条规则
- 元数据说明：`字段约束说明`、`关联逻辑说明`
- 验证设计：`校验场景覆盖矩阵`，覆盖单表校验、多表关联核对、指标一致性校验

这说明赛题5不再只是抽象命题，而是已经有一套可直接落地的演示数据。异常样例也非常适合展示：例如实付金额大于订单金额、用户ID为空、金额非数值、订单明细金额不一致、重复支付、库存扣减不一致、下架商品仍出库等。

推荐方案可以定位为：

> 面向测试与准生产数据的业务准确性智能核验平台，通过规则引擎 + AI 异常归因 + 修复建议生成，帮助测试人员快速发现跨表、跨指标、跨流程的数据质量问题。

最小可交付版本建议包括：

1. Excel 多 sheet 数据导入与表结构识别
2. 规则库解析，先支持内置规则执行，再扩展自然语言规则解释
3. 单表字段约束校验、金额逻辑校验、跨表关联校验
4. 输出异常清单、命中规则、严重等级、异常原因、修复建议
5. 展示规则覆盖率、异常数量、严重/警告分布、校验耗时

AI 亮点建议放在三处：

1. 将业务规则自动转为可执行校验逻辑或半自动生成校验模板
2. 对异常做自然语言归因，说明影响范围和优先级
3. 生成可执行修复建议，例如修复 SQL、人工核查步骤、上游数据修正建议

### 赛题2：排名上升，适合稳妥完赛

赛题2的输入案例比预期更完整。目录中有 5 组 `dat.gz + flg` 文件：

- `user`：2000 行
- `employee`：800 行
- `product`：1500 行
- `order`：3000 行
- `transaction`：5000 行

`flg` 文件包含文件名、行数、建表来源 SQL、定长行长度、字段数量和字段区间。例如字段定义采用 `序号$$字段名$$类型$$(起始,结束)`，非常适合做通用定长文件解析、结构校验、自动建表和批量入库。

赛题2的优势是边界清楚、可验证性强、工程闭环稳定。可以直接展示文件检查、字段切分、类型转换、入库记录数、成功率、耗时、加载速率和异常报告。

但它的短板仍然是 AI 必要性偏弱。除非加入以下增强，否则更像高质量 ETL 工具：

1. 自动识别 flg 元数据并生成建表 SQL
2. 智能诊断行长不一致、字段越界、类型转换失败、行数不匹配
3. 根据数据规模和字段类型推荐批量加载策略
4. 对失败原因生成可读报告和修复建议

因此，赛题2适合“稳妥完赛”，但冲奖上限通常低于赛题5。

### 赛题3：创新仍强，但风险没有下降

赛题3输入案例是电商订单系统，包含 `users`、`products`、`orders` 三张表，给出了主外键关系、字段约束、数据生成规则和数据量要求：

- `users`：100 条
- `products`：50 条
- `orders`：500 条

案例清晰，适合做测试数据生成演示。它的优势是 AI 亮点天然较强：可以从表结构和规则中推断生成顺序、生成策略、字段取值、关联约束和校验报告。

但风险也很明确：生成数据不仅要“看起来像”，还要满足唯一性、外键、金额计算、时间约束、状态分布、库存逻辑等约束。若做不到稳定校验闭环，演示时容易暴露可信度问题。

建议把赛题3作为第二创新备选，而不是稳妥首选。若选择它，应优先做窄范围高质量闭环：

1. 规则解析
2. 按依赖拓扑生成主表再生成从表
3. 生成后自动校验
4. 对失败规则自动重试或修复
5. 输出关联匹配度、规则通过率、生成耗时

### 赛题4：最容易做完整，但输入支撑不足

赛题4的输入案例是 `赛题4-支持敏感信息脱敏的通用工具-测试数据v0.3.xlsx`，有 1345 行左右测试数据，字段包括姓名、手机号、身份证号、银行卡号、地址、国家，并包含中文、英文、日文、韩文、法文等多语言姓名和地址。

这对展示脱敏效果很友好，尤其适合展示多语言、多字段、多规则处理。但当前案例只提供了测试数据，没有看到配套的脱敏规则标准文件。因此规则需要自行设计，亮点主要来自工程完整度，而不是赛题输入本身。

适合做的增强包括：

1. 配置化规则：姓名、手机号、身份证、银行卡、地址分字段脱敏
2. 注解式脱敏：支持对象字段、接口返回、日志打印
3. 规则覆盖率报告和脱敏前后 diff
4. 敏感字段自动识别和规则推荐

赛题4非常稳，但赛题成熟度高、同质化明显，除非把“自动识别 + 注解/配置统一 + 日志链路脱敏 + 审计报告”做完整，否则不建议作为冲奖首选。

### 赛题1：新增案例反而降低推荐度

赛题1输入案例是贷款评分服务伪代码和缺失需求文档片段。它确实能演示“代码理解 -> 文档补全 -> 注释补全”，但存在几个问题：

1. 案例明确提示可自行设计输入输出，约束不强
2. 代码片段有重复变量和不完整逻辑，收入规则在代码与输出说明之间不完全一致
3. 大模型生成文档是常见能力，评委容易认为同质化
4. 输出质量难以客观量化，容易被质疑是否幻觉或过度补充

因此，赛题1适合快速做原型，不适合作为主攻题。若必须选择，应把重点放在“代码-文档一致性检测”和“需求变更差异追踪”，而不是单纯文档生成。

## 最终建议

首选仍是 **赛题5**。新增输入案例已经给了完整规则、完整数据、异常标签和场景矩阵，最适合做可运行、可复现、可核验的作品，也最容易体现 AI 在规则理解、异常归因和修复建议上的价值。

如果团队更看重交付稳定性，可以把 **赛题2** 作为保底方案；如果团队更看重创新表达且有足够实现时间，可以把 **赛题3** 作为挑战方案。

推荐决策：

1. 主攻赛题5
2. 赛题5中轻度吸收赛题3能力：增加“异常样本生成 / 规则反例生成”作为辅助亮点
3. 不建议把赛题2、4、1作为冲奖主线，除非时间很短或团队更偏工程工具交付
