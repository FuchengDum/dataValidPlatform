# 赛题5：业务数据准确性验证工具需求文档

## 1. 文档说明

本文档基于《研发测试提效率赛道》赛题说明、《赛题5：业务数据准确性验证工具分析与简单设计》以及输入案例 `各赛题输入案例/赛题5-业务数据准确性验证工具-输入案例.xlsx` 编写，用于明确业务数据准确性验证工具的建设目标、功能范围、数据输入、输出结果、模块设计、验收标准和实施边界。

工具首版定位为面向比赛交付的 MVP，不建设大而全的数据质量平台。核心目标是围绕赛题5提供的 Excel 数据、业务规则库、字段约束说明、关联逻辑说明和校验场景矩阵，形成可运行、可复现、可核验的业务数据准确性验证闭环。

## 2. 项目背景与业务痛点

### 2.1 赛题背景

研发测试提效率赛道聚焦通过 AI 技术解决研发测试过程中的效率和质量问题。赛题5要求设计并开发一款业务数据准确性保障工具，该工具可通过业务规则定义进行数据准确性核对，并输出差错信息、差错原因和修复方案。

赛题5输入材料包含正常及异常场景的业务测试数据集、标准业务规则库、字段约束与关联逻辑说明，覆盖单表校验、多表关联核对、指标一致性校验等典型验证场景。输出成果需要包含基于业务规则的核对结果、异常业务疑点清单、异常成因分析及可执行修复方案，并提供工具运行界面、业务规则校验日志及疑点核查截图。

### 2.2 业务痛点

当前业务系统在测试数据、准生产数据和业务核对场景中，常见问题不是数据能否被读取，而是数据是否符合业务事实。典型痛点如下：

1. 数据准确性缺少统一核验手段：业务数据通常分布在订单、明细、商品、支付、库存等多张表中，仅检查字段格式无法发现业务链路错误。
2. 跨表异常定位成本高：订单金额、明细小计、支付金额和库存扣减之间存在强关联，人工核对需要频繁跨表查询和聚合。
3. 异常原因不直观：单条记录可能看似合法，但放到订单履约、支付对账或库存扣减链路中会暴露一致性问题。
4. 修复依赖业务理解：测试人员不仅需要知道哪条数据异常，还需要知道异常影响什么、为什么发生、应该如何核查和修复。
5. 规则资产复用不足：业务规则散落在文档、SQL、测试经验和人工检查步骤中，缺少统一执行、统一展示和统一沉淀。

## 3. 建设目标

### 3.1 总体目标

建设一款业务数据准确性智能验证工具，通过规则库驱动数据核验，自动发现单表字段异常、多表关联异常和指标一致性异常，并输出异常疑点清单、异常原因、影响说明、修复建议和汇总报告。

工具应支撑以下完整闭环：

```text
Excel 数据导入
  -> 表结构识别
  -> 规则库解析
  -> 规则执行
  -> 异常归并
  -> 原因解释
  -> 修复建议
  -> 报告展示
```

### 3.2 分阶段目标

| 优先级 | 阶段 | 目标 |
|---|---|---|
| P0 | 离线核验闭环 | 支持导入赛题5 Excel、识别业务数据和规则资产、执行核心规则、输出异常清单和统计报告 |
| P1 | 演示页面 | 提供数据导入、校验总览、异常列表、异常详情、规则覆盖统计和筛选能力 |
| P2 | AI 解释与修复建议 | 根据规则、异常记录和关联逻辑生成自然语言原因解释、影响分析和修复建议 |
| P3 | 规则模板化扩展 | 支持常见规则模板配置，AI 辅助推荐规则类型、字段映射和关联表 |

### 3.3 成功标准

1. 可运行：工具可以在本地或演示环境独立启动，并读取赛题5输入 Excel。
2. 可复现：同一输入数据多次执行得到一致的规则执行结果、异常数量和报告内容。
3. 可核验：异常结果可追溯到规则编号、异常表、异常记录、实际值、期望值和关联证据。
4. 可解释：异常原因和修复建议面向测试人员可理解、可操作。
5. 可展示：能够通过页面、报告、日志和截图支撑比赛答辩。

## 4. 用户角色与使用场景

### 4.1 用户角色

| 角色 | 关注点 | 主要操作 |
|---|---|---|
| 测试人员 | 快速发现测试数据中的业务错误 | 导入数据、执行校验、查看异常、导出报告 |
| 测试负责人 | 评估数据质量和规则覆盖情况 | 查看统计总览、严重等级分布、规则命中情况 |
| 业务分析人员 | 理解异常原因和影响范围 | 查看业务链路、规则说明、修复建议 |
| 开发人员 | 根据疑点修复数据或调整造数逻辑 | 查看异常证据、核查步骤、修复 SQL 草案 |
| 比赛评委 | 判断工具价值、完整度和可复现性 | 查看演示页面、运行日志、异常截图和导出报告 |

### 4.2 核心使用场景

1. 测试人员导入赛题5 Excel 输入案例。
2. 工具自动识别 5 张业务数据表、30 条业务规则、字段约束、关联逻辑和 15 个校验场景。
3. 测试人员点击开始校验，工具执行单表字段约束、单表业务规则、多表关联核对和指标一致性校验。
4. 工具输出异常疑点清单，并按规则类型、业务表、严重等级、校验场景展示统计分布。
5. 测试人员选择某条异常，查看异常记录、命中规则、实际值、期望值、关联证据、原因分析和修复建议。
6. 测试人员导出核验报告，用于问题跟踪、答辩材料和后续开发修复。

## 5. 输入数据与规则资产分析

### 5.1 输入文件

首版标准输入文件为：

```text
各赛题输入案例/赛题5-业务数据准确性验证工具-输入案例.xlsx
```

工具应支持读取该 Excel 文件中的多 sheet 数据，并按业务数据、规则资产、元数据、验证设计和说明信息分类处理。

### 5.2 Sheet 清单

| 类型 | Sheet | 说明 |
|---|---|---|
| 业务数据 | `订单表_t_order` | 订单主表，包含订单状态、订单金额、实付金额、优惠金额、下单时间、支付时间等字段 |
| 业务数据 | `订单明细表_t_order_item` | 订单明细表，包含商品、单价、数量、小计金额等字段 |
| 业务数据 | `商品表_t_product` | 商品基础表，包含成本价、售价、库存数量、上架状态等字段 |
| 业务数据 | `支付表_t_payment` | 支付记录表，包含支付金额、支付状态、退款金额等字段 |
| 业务数据 | `库存流水表_t_inventory_log` | 库存变动流水表，包含出入库数量、变动前后库存、关联订单等字段 |
| 规则资产 | `业务规则库` | R001 到 R030，共 30 条业务规则 |
| 元数据 | `字段约束说明` | 字段类型、必填、唯一性、取值范围、默认值和约束说明 |
| 元数据 | `关联逻辑说明` | 订单、明细、商品、支付、库存流水之间的关联关系和核验要点 |
| 验证设计 | `校验场景覆盖矩阵` | S001 到 S015，共 15 个校验场景 |
| 说明 | `使用说明` | 输入案例说明 |

### 5.3 业务数据规模

| 数据表 | 数据行数 | 正常行数 | 异常行数 | 主要核验关注点 |
|---|---:|---:|---:|---|
| `t_order` | 15 | 5 | 10 | 必填字段、金额关系、状态时间、状态流转 |
| `t_order_item` | 15 | 6 | 9 | 明细金额、数量、商品存在性、商品价格 |
| `t_product` | 14 | 7 | 7 | 成本价、售价、库存、上架状态 |
| `t_payment` | 15 | 4 | 11 | 支付金额、支付状态、重复支付、订单用户一致性 |
| `t_inventory_log` | 11 | 4 | 7 | 库存变动连续性、出入库数量、下架商品出库 |

### 5.4 规则资产规模

| 规则类型 | 数量 | 说明 |
|---|---:|---|
| 单表校验-字段约束 | 8 | 检查非空、非负、类型、数量等字段约束 |
| 单表校验-业务规则 | 8 | 检查金额关系、库存规则、售价成本关系等业务规则 |
| 多表关联核对 | 8 | 检查订单、明细、商品、支付、库存之间的关联一致性 |
| 指标一致性校验 | 6 | 检查时间逻辑、状态流转、重复支付、库存变动等指标一致性 |

严重等级分布为严重 25 条、警告 5 条。工具应在页面和报告中突出严重异常，便于优先处理资金、库存和关键业务链路风险。

### 5.5 规则清单

| 规则编号 | 规则名称 | 规则分类 | 适用表 | 严重等级 |
|---|---|---|---|---|
| R001 | 金额非负校验 | 单表校验-字段约束 | `t_order` | 严重 |
| R002 | 必填字段非空校验 | 单表校验-字段约束 | `t_order` | 严重 |
| R003 | 金额类型校验 | 单表校验-字段约束 | `t_order` | 严重 |
| R004 | 优惠金额合理性校验 | 单表校验-业务规则 | `t_order` | 警告 |
| R005 | 订单金额非零校验 | 单表校验-业务规则 | `t_order` | 严重 |
| R006 | 实付金额与订单金额关系校验 | 单表校验-业务规则 | `t_order` | 严重 |
| R007 | 商品售价成本价关系校验 | 单表校验-业务规则 | `t_product` | 警告 |
| R008 | 库存非负校验 | 单表校验-字段约束 | `t_product` | 严重 |
| R009 | 上架商品库存校验 | 单表校验-业务规则 | `t_product` | 警告 |
| R010 | 商品价格非零校验 | 单表校验-字段约束 | `t_product` | 严重 |
| R011 | 明细小计金额校验 | 单表校验-业务规则 | `t_order_item` | 严重 |
| R012 | 明细数量校验 | 单表校验-字段约束 | `t_order_item` | 严重 |
| R013 | 支付金额非负校验 | 单表校验-字段约束 | `t_payment` | 严重 |
| R014 | 支付金额零值校验 | 单表校验-业务规则 | `t_payment` | 严重 |
| R015 | 库存变动连续性校验 | 单表校验-业务规则 | `t_inventory_log` | 严重 |
| R016 | 入库数量正数校验 | 单表校验-字段约束 | `t_inventory_log` | 严重 |
| R017 | 订单-明细金额一致性 | 多表关联核对 | `t_order`、`t_order_item` | 严重 |
| R018 | 明细-商品关联校验 | 多表关联核对 | `t_order_item`、`t_product` | 严重 |
| R019 | 明细-商品价格一致性 | 多表关联核对 | `t_order_item`、`t_product` | 警告 |
| R020 | 订单-支付金额一致性 | 多表关联核对 | `t_order`、`t_payment` | 严重 |
| R021 | 订单-支付状态一致性 | 多表关联核对 | `t_order`、`t_payment` | 严重 |
| R022 | 订单-库存扣减一致性 | 多表关联核对 | `t_order_item`、`t_inventory_log` | 严重 |
| R023 | 支付-订单关联存在性 | 多表关联核对 | `t_payment`、`t_order` | 严重 |
| R024 | 支付-订单用户一致性 | 多表关联核对 | `t_payment`、`t_order` | 严重 |
| R025 | 订单状态时间逻辑校验 | 指标一致性校验 | `t_order` | 严重 |
| R026 | 订单状态流转校验 | 指标一致性校验 | `t_order` | 严重 |
| R027 | 库存变动后非负校验 | 指标一致性校验 | `t_inventory_log` | 严重 |
| R028 | 下架商品出库校验 | 指标一致性校验 | `t_inventory_log`、`t_product` | 警告 |
| R029 | 同一订单重复支付校验 | 指标一致性校验 | `t_payment` | 严重 |
| R030 | 订单金额与明细汇总一致性 | 指标一致性校验 | `t_order`、`t_order_item` | 严重 |

### 5.6 关联逻辑

| 关联编号 | 主表 | 从表 | 关联字段 | 校验要点 |
|---|---|---|---|---|
| REL001 | `t_order` | `t_order_item` | 订单ID | 订单金额等于明细小计金额之和，明细订单ID必须在订单表中存在 |
| REL002 | `t_product` | `t_order_item` | 商品ID | 明细商品ID必须存在，明细单价应与商品售价一致或符合下单快照口径 |
| REL003 | `t_order` | `t_payment` | 订单ID | 实付金额等于支付成功金额之和，已支付订单必须存在支付成功记录，同一订单不应重复支付 |
| REL004 | `t_order` | `t_payment` | 用户ID | 支付表通过订单ID关联订单表后，用户ID必须匹配 |
| REL005 | `t_product` | `t_inventory_log` | 商品ID | 库存变动后数量应与商品表当前库存一致，下架商品不应有出库记录 |
| REL006 | `t_order` | `t_inventory_log` | 订单ID | 已支付订单明细商品应有对应出库记录，出库数量应与明细数量一致 |
| REL007 | `t_order_item` | `t_inventory_log` | 订单ID+商品ID | 出库流水的变动数量应等于订单明细数量，出库商品ID应等于明细商品ID |

## 6. 功能需求

### 6.1 P0 Excel 导入与表结构识别

**需求描述**：工具应支持导入赛题5 Excel 文件，读取所有 sheet，并识别业务数据、业务规则、字段约束、关联逻辑和校验场景。

**输入**：

- 赛题5 Excel 文件。
- Excel 中的 10 个 sheet。

**处理规则**：

1. 读取 Excel 文件并校验文件是否存在、是否可解析。
2. 识别固定 sheet 名称，并映射为内部数据对象。
3. 对业务数据表读取表头和数据行。
4. 对规则资产读取规则编号、规则名称、规则分类、适用表、校验逻辑、严重等级和异常示例。
5. 对字段约束、关联逻辑和场景矩阵读取元数据，用于规则执行和报告说明。

**输出**：

- 业务数据表集合。
- 规则库集合。
- 字段约束集合。
- 关联逻辑集合。
- 校验场景集合。
- 导入结果摘要。

**验收标准**：

- 能识别 5 张业务数据表。
- 能识别 R001 到 R030 共 30 条规则。
- 能识别字段约束、7 条关联逻辑和 S001 到 S015 共 15 个校验场景。
- 导入失败时给出明确错误信息，不进入规则执行。

### 6.2 P0 规则管理

**需求描述**：工具应维护业务规则的标准结构，支持按规则编号、规则分类、适用表、严重等级和场景编号组织规则。

**输入**：

- `业务规则库` sheet。
- `校验场景覆盖矩阵` sheet。

**处理规则**：

1. 将规则编号作为规则唯一标识。
2. 将规则分类标准化为单表校验-字段约束、单表校验-业务规则、多表关联核对、指标一致性校验。
3. 将规则严重等级标准化为严重、警告。
4. 根据场景矩阵建立规则与场景的映射关系。
5. 首版采用内置规则执行方式，R001 到 R030 映射到对应校验函数或规则模板。

**输出**：

- 可执行规则列表。
- 规则覆盖统计。
- 未执行规则列表。

**验收标准**：

- 规则总数统计为 30。
- 页面和报告中可以按规则分类、严重等级、适用表筛选或统计。
- 规则执行结果能够回填规则名称、规则分类、严重等级和场景编号。

### 6.3 P0 单表字段约束校验

**需求描述**：工具应支持针对单张表执行字段级校验，发现非空、非负、数值类型、数量正数、价格非零等异常。

**覆盖规则**：

- R001 金额非负校验。
- R002 必填字段非空校验。
- R003 金额类型校验。
- R008 库存非负校验。
- R010 商品价格非零校验。
- R012 明细数量校验。
- R013 支付金额非负校验。
- R016 入库数量正数校验。

**输入**：

- 业务数据表。
- 字段约束说明。
- 对应规则定义。

**处理规则**：

1. 对必填字段检查空值、空字符串和明显缺失值。
2. 对金额、库存、数量等字段检查数值类型和取值范围。
3. 对违反字段约束的记录生成异常结果。

**输出**：

- 字段约束异常清单。

**验收标准**：

- S001 检出用户ID为空、收货地址为空等必填异常。
- S002 检出订单金额负数、商品库存负数、成本价负数等数值范围异常。
- S004 检出订单金额为非数值内容的类型异常。

### 6.4 P0 单表业务规则校验

**需求描述**：工具应支持针对单张表内部字段之间的业务关系执行校验。

**覆盖规则**：

- R004 优惠金额合理性校验。
- R005 订单金额非零校验。
- R006 实付金额与订单金额关系校验。
- R007 商品售价成本价关系校验。
- R009 上架商品库存校验。
- R011 明细小计金额校验。
- R014 支付金额零值校验。
- R015 库存变动连续性校验。

**输入**：

- 业务数据表。
- 业务规则定义。

**处理规则**：

1. 校验订单实付金额、订单金额和优惠金额之间的关系。
2. 校验商品售价、成本价、库存和上架状态之间的业务关系。
3. 校验订单明细小计金额是否等于单价乘以数量。
4. 校验支付成功记录的支付金额是否符合有效支付要求。
5. 校验库存流水中变动前库存、变动数量和变动后库存之间的连续关系。

**输出**：

- 单表业务规则异常清单。

**验收标准**：

- S003 检出实付金额大于订单金额、优惠金额比例异常等金额关系异常。
- S005 检出明细小计金额错误、明细数量异常。
- S015 检出商品售价低于成本价、上架商品库存异常、商品价格为零等异常。

### 6.5 P0 多表关联核对

**需求描述**：工具应支持订单、订单明细、商品、支付、库存流水之间的跨表关联核对。

**覆盖规则**：

- R017 订单-明细金额一致性。
- R018 明细-商品关联校验。
- R019 明细-商品价格一致性。
- R020 订单-支付金额一致性。
- R021 订单-支付状态一致性。
- R022 订单-库存扣减一致性。
- R023 支付-订单关联存在性。
- R024 支付-订单用户一致性。

**输入**：

- 多张业务数据表。
- 关联逻辑说明。
- 业务规则定义。

**处理规则**：

1. 按订单ID聚合订单明细小计金额，与订单金额核对。
2. 检查订单明细中的商品ID是否存在于商品表。
3. 检查订单明细单价与商品售价是否一致或符合下单快照口径。
4. 按订单ID聚合支付成功金额，与订单实付金额核对。
5. 检查已支付订单是否存在支付成功记录。
6. 检查支付记录关联订单是否存在，用户ID是否一致。
7. 按订单ID和商品ID核对出库流水与订单明细数量。

**输出**：

- 多表关联异常清单。
- 关联证据，包括主表记录、从表记录、聚合值和对比结果。

**验收标准**：

- S006 检出订单金额与明细金额不一致。
- S007 检出明细商品ID不存在或关联缺失。
- S008 检出订单与支付金额不一致、支付用户不一致。
- S009 检出支付状态与订单状态不一致、支付关联订单缺失。
- S010 检出订单库存扣减不一致。

### 6.6 P0 指标一致性校验

**需求描述**：工具应支持面向关键业务指标和业务状态的综合一致性校验。

**覆盖规则**：

- R025 订单状态时间逻辑校验。
- R026 订单状态流转校验。
- R027 库存变动后非负校验。
- R028 下架商品出库校验。
- R029 同一订单重复支付校验。
- R030 订单金额与明细汇总一致性。

**输入**：

- 业务数据表。
- 规则定义。
- 关联逻辑。

**处理规则**：

1. 检查订单支付时间不得早于下单时间。
2. 检查订单状态是否符合允许的业务流转范围。
3. 检查库存变动后数量不得为负。
4. 检查下架商品不应存在出库记录。
5. 检查同一订单是否存在重复支付成功记录。
6. 从指标口径再次核对订单金额与明细汇总金额一致性。

**输出**：

- 指标一致性异常清单。

**验收标准**：

- S011 检出订单状态与时间逻辑异常。
- S012 检出订单状态流转异常。
- S013 检出同一订单重复支付。
- S014 检出下架商品出库。

### 6.7 P0 异常结果生成

**需求描述**：工具应将所有规则命中的异常统一转换为标准异常结果模型。

**异常结果字段**：

| 字段 | 说明 |
|---|---|
| `rule_id` | 规则编号，例如 R006 |
| `rule_name` | 规则名称 |
| `rule_type` | 规则分类 |
| `severity` | 严重或警告 |
| `table_name` | 异常主表 |
| `record_key` | 异常记录主键，例如 ORD006 |
| `field_name` | 涉及字段，可为空 |
| `actual_value` | 实际值 |
| `expected_value` | 期望值或规则条件 |
| `description` | 异常描述 |
| `reason` | 异常原因解释 |
| `impact` | 影响范围说明 |
| `suggestion` | 修复建议 |
| `scenario_id` | 对应场景编号，例如 S003 |
| `evidence` | 关联证据，包括关联记录、聚合值或计算过程 |

**输出**：

- 标准异常结果集合。

**验收标准**：

- 每条异常至少包含规则编号、规则名称、异常表、异常主键、严重等级、异常描述、异常原因和修复建议。
- 多表异常必须包含关联证据。
- 同一异常在页面、报告和导出文件中的字段含义一致。

### 6.8 P0 汇总报告

**需求描述**：工具应输出规则执行统计和异常统计报告。

**报告内容**：

1. 总规则数。
2. 已执行规则数。
3. 未执行规则数。
4. 命中异常数。
5. 严重/警告分布。
6. 按表分布。
7. 按规则类型分布。
8. 按场景分布。
9. 校验开始时间、结束时间和耗时。
10. 异常明细表。

**输出形式**：

- Markdown 报告。
- Excel 报告或 HTML 报告。
- 页面统计卡片。

**验收标准**：

- 报告能够独立阅读，包含输入文件、执行结果、异常明细和统计摘要。
- 报告中的异常总数与异常清单数量一致。
- 报告中的规则总数与规则库数量一致。

### 6.9 P1 可视化页面

**需求描述**：工具应提供适合比赛展示的轻量页面，展示导入、校验、总览、异常列表和异常详情。

**页面区域**：

1. 数据导入区：上传或选择 Excel 文件，展示识别到的表、规则数和场景数。
2. 校验总览区：展示总规则数、已执行规则数、异常总数、严重异常数、警告数和耗时。
3. 异常清单区：展示异常列表，支持按规则、表、严重等级、规则类型和场景筛选。
4. 异常详情区：展示异常记录、命中规则、关联证据、原因解释、影响范围和修复建议。
5. 报告导出区：支持导出核验报告和异常清单。

**验收标准**：

- 页面可以完成从导入到查看异常详情的完整演示。
- 点击异常记录可以查看原因分析和修复建议。
- 筛选条件生效后，统计数量与列表结果一致。

### 6.10 P2 AI 辅助分析

**需求描述**：工具应将 AI 能力用于异常解释、影响分析、修复建议和规则模板推荐，不将 AI 作为基础校验正确性的唯一依据。

**能力范围**：

1. 异常原因解释：根据规则描述、异常记录、实际值、期望值和关联证据生成自然语言解释。
2. 影响范围说明：说明异常可能影响财务对账、订单履约、库存准确性、支付核查或测试用例执行。
3. 修复建议生成：输出人工核查步骤、字段修正建议或修复 SQL 草案。
4. 规则模板推荐：根据规则描述和伪 SQL 推荐规则类型、字段映射和关联表。

**约束**：

- AI 生成内容必须标注为建议。
- 修复 SQL 不自动执行。
- AI 输出应保留原始规则和证据链，便于人工复核。
- 当 AI 服务不可用时，工具应使用内置模板生成基础原因和建议，不影响规则执行。

**验收标准**：

- 对典型异常能生成可读原因、影响说明和修复建议。
- AI 关闭或失败时，规则执行和异常报告仍可正常输出。
- 修复建议不会自动修改输入数据。

### 6.11 P3 规则模板化扩展

**需求描述**：工具应为后续扩展预留规则模板能力，使新增规则可以优先通过模板参数配置，而不是每次硬编码。

**模板类型**：

1. 非空校验。
2. 非负校验。
3. 枚举值校验。
4. 数值类型校验。
5. 字段等式校验。
6. 聚合一致性校验。
7. 跨表存在性校验。
8. 跨表字段一致性校验。
9. 重复记录校验。
10. 状态时间逻辑校验。

**验收标准**：

- 文档和代码结构能明确区分内置规则与模板化规则。
- 后续新增规则时，可以复用已有模板并配置表名、字段名、关联字段和比较逻辑。

## 7. 非功能需求

### 7.1 可运行性

工具应支持在本地演示环境独立运行。依赖项、启动命令、输入文件路径和输出报告路径应在 README 或运行说明中清晰描述。

### 7.2 可复现性

同一输入 Excel、同一规则版本、同一配置下，多次执行应得到一致的异常清单、统计结果和报告内容。规则执行结果不应依赖随机数或不可控外部状态。

### 7.3 可核验性

每条异常必须能够追溯到：

1. 命中的规则编号。
2. 异常记录主键。
3. 实际值和期望值。
4. 相关业务表和字段。
5. 跨表异常的关联证据。
6. 对应校验场景。

### 7.4 易用性

测试人员应能在不理解底层代码的情况下完成导入、执行校验、筛选异常、查看详情和导出报告。页面文案应使用业务语言，避免只展示技术字段名。

### 7.5 性能要求

赛题5输入案例规模较小，首版以准确性和可解释性优先。对于当前输入案例，数据导入、规则执行和报告生成应在可演示的短时间内完成，并记录校验耗时。

### 7.6 安全与边界

工具不应硬编码真实生产数据、密钥或凭证。AI 修复建议和 SQL 草案仅用于人工参考，不直接自动执行数据库修改。

## 8. 系统模块设计

### 8.1 模块划分

| 模块 | 职责 |
|---|---|
| 数据导入模块 | 读取 Excel 多 sheet，转换为统一的内存表结构 |
| 元数据解析模块 | 解析字段约束、关联逻辑和校验场景覆盖矩阵 |
| 规则管理模块 | 加载业务规则库，维护规则编号、分类、适用表、严重等级和场景映射 |
| 规则执行模块 | 按规则类型执行校验，生成标准异常结果 |
| 异常归并模块 | 对规则命中结果进行规范化、去重、分组和证据补充 |
| AI 辅助分析模块 | 根据异常记录、规则描述和关联说明生成原因解释、影响范围和修复建议 |
| 报告展示模块 | 输出异常清单、统计摘要、规则覆盖率、日志和演示页面 |

### 8.2 数据流

```text
赛题5 Excel
  |
  v
数据导入模块
  |
  +--> 业务数据表集合
  +--> 业务规则库
  +--> 字段约束说明
  +--> 关联逻辑说明
  +--> 校验场景矩阵
  |
  v
元数据解析模块 + 规则管理模块
  |
  v
规则执行模块
  |
  v
异常归并模块
  |
  +--> AI 辅助分析模块
  |
  v
报告展示模块
```

### 8.3 规则执行策略

首版采用分层规则执行策略：

1. 第一层：结构化内置规则。对 R001 到 R030 中的核心规则做显式实现，保证结果稳定、可控、易演示。
2. 第二层：规则模板化。将非空、非负、枚举值、金额等式、跨表存在性、聚合一致性等常见规则抽象成模板。
3. 第三层：AI 辅助转换。AI 根据规则描述推荐规则模板和字段映射，生成结果需要人工确认或由测试样例验证，不直接执行不可信代码。

## 9. 页面与报告设计

### 9.1 页面信息架构

页面首屏应直接展示工具主体能力，不做营销式介绍。建议页面结构如下：

1. 顶部操作栏：文件选择、开始校验、导出报告。
2. 数据识别摘要：业务表数量、规则数量、场景数量、字段约束数量、关联逻辑数量。
3. 校验统计区：异常总数、严重异常数、警告异常数、已执行规则数、校验耗时。
4. 分布分析区：按表、规则类型、严重等级、场景展示分布。
5. 异常列表区：展示异常清单并提供筛选。
6. 异常详情区：展示证据链、原因、影响和修复建议。

### 9.2 异常列表字段

| 字段 | 说明 |
|---|---|
| 规则编号 | 命中的规则编号 |
| 规则名称 | 命中的规则名称 |
| 严重等级 | 严重或警告 |
| 异常表 | 异常主表 |
| 异常主键 | 异常记录标识 |
| 异常描述 | 简要说明异常内容 |
| 场景编号 | 对应校验场景 |
| 状态 | 待核查、已确认、已忽略，可作为后续扩展 |

### 9.3 异常详情内容

1. 基本信息：规则编号、规则名称、规则分类、严重等级。
2. 命中数据：异常表、记录主键、字段名、实际值、期望值。
3. 关联证据：关联表记录、聚合计算过程、对比结果。
4. 原因解释：说明为什么该数据不符合业务规则。
5. 影响范围：说明可能影响的业务环节。
6. 修复建议：给出人工核查步骤或修复 SQL 草案。

### 9.4 报告格式

报告应至少包含以下章节：

1. 报告摘要。
2. 输入文件信息。
3. 规则执行概览。
4. 异常统计分析。
5. 异常明细清单。
6. 重点异常说明。
7. 修复建议汇总。
8. 校验日志摘要。

## 10. 校验场景与验收标准

| 场景编号 | 校验场景 | 覆盖规则 | 预期校验结果 |
|---|---|---|---|
| S001 | 单表字段非空校验 | R002 | 检出用户ID为空、收货地址为空等 2 条异常记录 |
| S002 | 单表数值范围校验 | R001、R008 | 检出订单金额负数、商品库存负数、成本价负数等 3 条异常记录 |
| S003 | 单表业务规则校验 | R006 | 检出实付金额大于订单金额、优惠金额比例异常等 2 条异常记录 |
| S004 | 单表字段类型校验 | R003 | 检出订单金额非数值异常 |
| S005 | 单表明细金额计算校验 | R011、R012 | 检出明细金额或数量相关异常 |
| S006 | 订单-明细金额一致性核对 | R017 | 检出订单金额与明细汇总金额不一致 |
| S007 | 明细-商品存在性核对 | R018 | 检出商品关联缺失异常 |
| S008 | 订单-支付金额一致性核对 | R020、R024 | 检出跨表金额或用户不一致异常 |
| S009 | 订单-支付状态一致性核对 | R021、R023 | 检出支付状态或订单关联异常 |
| S010 | 订单-库存扣减一致性核对 | R022 | 检出库存扣减与订单明细不一致 |
| S011 | 订单状态与时间逻辑一致性 | R025 | 检出支付时间早于下单时间等异常 |
| S012 | 订单状态流转一致性 | R026 | 检出订单状态流转异常 |
| S013 | 重复支付校验 | R029 | 检出同一订单重复支付成功异常 |
| S014 | 下架商品出库校验 | R028 | 检出下架商品仍发生出库异常 |
| S015 | 商品售价成本价一致性 | R007、R009、R010 | 检出售价低于成本价、上架库存异常、商品价格为零等异常 |

## 11. 演示路径

比赛展示建议按以下步骤执行：

1. 打开工具页面或运行工具命令。
2. 导入 `赛题5-业务数据准确性验证工具-输入案例.xlsx`。
3. 展示系统识别到 5 张业务表、30 条规则、15 个校验场景和 7 条关联逻辑。
4. 点击开始校验。
5. 展示异常总览，包括严重异常、警告异常、按表分布、按规则类型分布和校验耗时。
6. 展示单表字段异常，例如用户ID为空或金额非数值。
7. 展示单表业务异常，例如实付金额大于订单金额。
8. 展示跨表关联异常，例如订单金额与明细金额不一致。
9. 展示库存链路异常，例如出库数量与订单明细数量不一致或下架商品仍出库。
10. 展示 AI 生成的异常原因、影响范围和修复建议。
11. 导出核验报告，展示报告中的统计摘要和异常明细。

## 12. 风险与控制

| 风险 | 表现 | 控制方式 |
|---|---|---|
| 规则自动化过度承诺 | 自然语言规则无法稳定转成可执行逻辑 | 首版采用内置规则，AI 只做推荐、解释和建议 |
| 跨表校验实现复杂 | 多表聚合、重复支付、库存流水容易出现统计口径偏差 | 优先覆盖场景矩阵中的关键场景，逐条对齐预期结果 |
| AI 生成内容不可信 | 修复建议可能不准确或不完整 | 明确标注 AI 内容为建议，保留规则和证据链供人工复核 |
| 工具范围膨胀 | 逐步演变为通用数据质量平台，影响比赛交付 | MVP 只围绕赛题5输入和 R001 到 R030 规则闭环 |
| 演示效果不足 | 只有命令行输出，难体现业务价值 | 至少提供汇总报告和异常详情页，优先展示跨表链路异常 |
| 结果不可复现 | 多次执行结果不一致，影响答辩可信度 | 固化输入文件、规则版本和执行顺序，报告记录执行时间和规则总数 |

## 13. 实施边界

### 13.1 首版范围内

1. 支持赛题5 Excel 文件导入。
2. 支持识别固定 sheet 和固定字段结构。
3. 支持 R001 到 R030 规则的内置执行。
4. 支持异常清单、统计报告和演示页面。
5. 支持 AI 或模板化的异常原因与修复建议生成。
6. 支持导出 Markdown、Excel 或 HTML 形式的核验报告。

### 13.2 首版范围外

1. 不直接连接生产数据库。
2. 不自动执行修复 SQL。
3. 不承诺任意自然语言规则自动生成可执行代码。
4. 不建设完整的数据质量平台、权限系统、任务调度系统或多租户体系。
5. 不处理赛题5输入案例之外的大规模异构数据源。

## 14. 交付物清单

| 交付物 | 说明 |
|---|---|
| 可运行工具或组件 | 支持导入 Excel、执行规则、输出结果 |
| 工具运行界面 | 展示导入、总览、异常列表和异常详情 |
| 业务规则校验日志 | 记录规则加载、执行、命中情况和耗时 |
| 异常疑点清单 | 按标准异常结果模型输出 |
| 异常成因分析 | 对重点异常输出原因、影响和证据链 |
| 修复建议 | 输出人工核查步骤或修复 SQL 草案 |
| 核验报告 | 包含统计摘要、规则覆盖、异常明细和建议 |
| 演示截图 | 包含页面总览、异常详情、报告导出等关键截图 |

## 15. 总结

赛题5的关键价值在于把业务规则、测试数据、异常检出、原因解释和修复建议串成闭环。首版工具应坚持稳定、可控、可演示的实现策略：规则执行采用内置规则保障准确性，AI 能力聚焦异常解释、影响分析和修复建议，页面与报告突出规则覆盖、异常优先级和业务证据链。

最终验收重点不是工具是否覆盖所有数据质量场景，而是是否围绕赛题5输入资产稳定完成以下目标：

1. 规则执行结果可信。
2. 异常定位链路清楚。
3. 异常原因解释准确。
4. 修复建议可操作。
5. 展示过程顺畅、可复现、可核验。
