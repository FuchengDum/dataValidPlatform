# 赛题5：业务数据准确性验证工具分析与简单设计

## 1. 选择结论

基于已有《研发测试提效率赛道赛题选择分析》的结论，建议正式选择 **赛题5：业务数据准确性验证工具** 作为主攻方向。

核心原因是：赛题5同时具备明确业务痛点、完整输入案例、可验证规则库、可展示异常样本和较强 AI 增益空间。它比纯数据加载、脱敏或文档补全类工具更容易体现“业务理解 + 自动核验 + 异常解释 + 修复建议”的完整价值链。

推荐作品定位：

> 面向测试数据、准生产数据和业务核对场景的业务数据准确性智能验证工具。工具通过规则库驱动数据核验，自动发现单表字段异常、多表关联异常和指标一致性异常，并输出异常原因、影响说明和修复建议。

## 2. 输入案例分析

赛题5输入文件为 `各赛题输入案例/赛题5-业务数据准确性验证工具-输入案例.xlsx`，结构完整，适合作为首版工具的标准演示数据源。

输入文件包含 10 个 sheet：

| 类型 | Sheet | 说明 |
|---|---|---|
| 业务数据 | `订单表_t_order` | 订单主表，包含订单状态、订单金额、实付金额、优惠金额等核心字段 |
| 业务数据 | `订单明细表_t_order_item` | 订单明细表，包含商品、单价、数量、小计金额 |
| 业务数据 | `商品表_t_product` | 商品基础表，包含成本价、售价、库存数量、上架状态 |
| 业务数据 | `支付表_t_payment` | 支付记录表，包含支付金额、支付状态、退款金额 |
| 业务数据 | `库存流水表_t_inventory_log` | 库存变动流水，包含出入库数量、变动前后库存、关联订单 |
| 规则资产 | `业务规则库` | R001 到 R030，共 30 条规则 |
| 元数据 | `字段约束说明` | 字段类型、必填、唯一性、取值范围、默认值 |
| 元数据 | `关联逻辑说明` | 订单、明细、商品、支付、库存流水之间的关联逻辑 |
| 验证设计 | `校验场景覆盖矩阵` | S001 到 S015，共 15 个校验场景 |
| 说明 | `使用说明` | 输入案例说明 |

从数据规模看，案例虽小，但覆盖面完整：

| 数据表 | 数据行数 | 正常行数 | 异常行数 |
|---|---:|---:|---:|
| `t_order` | 15 | 5 | 10 |
| `t_order_item` | 15 | 6 | 9 |
| `t_product` | 14 | 7 | 7 |
| `t_payment` | 15 | 4 | 11 |
| `t_inventory_log` | 11 | 4 | 7 |

规则覆盖也比较均衡：

| 规则类型 | 数量 |
|---|---:|
| 单表校验-字段约束 | 8 |
| 单表校验-业务规则 | 8 |
| 多表关联核对 | 8 |
| 指标一致性校验 | 6 |

严重等级分布为：严重 25 条，警告 5 条。这有利于在展示时突出问题优先级和风险分层。

## 3. 业务价值分析

赛题5要解决的核心问题不是“数据格式是否能读”，而是“数据是否符合业务事实”。这类问题通常具有三个特点：

1. 异常跨表产生：例如订单金额、订单明细金额、支付金额、库存出库数量需要互相一致。
2. 异常原因不直观：单条记录看起来可能合法，但放到完整业务链路中会暴露问题。
3. 修复需要业务解释：测试人员不仅要知道哪条数据错了，还要知道为什么错、影响什么、应该如何修。

因此，本赛题的展示重点应放在业务闭环上：

```text
Excel 数据导入
  -> 表结构识别
  -> 规则库解析
  -> 规则执行
  -> 异常归并
  -> 原因解释
  -> 修复建议
  -> 报告展示
```

## 4. 最小可交付范围

首版工具建议控制为一个可运行、可复现、可演示的轻量工具，不做过度平台化。

MVP 目标：

1. 支持导入赛题5 Excel 文件。
2. 自动识别业务数据 sheet、规则库 sheet、字段约束 sheet、关联逻辑 sheet。
3. 支持执行三类核心校验：
   - 单表字段约束校验
   - 多表关联一致性校验
   - 指标一致性校验
4. 输出异常清单，至少包含：
   - 规则编号
   - 规则名称
   - 异常表
   - 异常主键
   - 严重等级
   - 异常描述
   - 异常原因
   - 修复建议
5. 输出汇总报告，至少包含：
   - 总规则数
   - 已执行规则数
   - 命中异常数
   - 严重/警告分布
   - 按表分布
   - 按场景分布
   - 校验耗时

首版可以先支持内置规则执行，即把 R001 到 R030 中的核心规则映射为可执行校验逻辑。自然语言规则自动生成代码可以作为增强能力，而不是第一阶段的硬依赖。

## 5. 简单设计

### 5.1 模块划分

建议将工具拆成 6 个模块：

| 模块 | 职责 |
|---|---|
| 数据导入模块 | 读取 Excel 多 sheet，转换为统一的内存表结构 |
| 元数据解析模块 | 解析字段约束、关联逻辑和校验场景覆盖矩阵 |
| 规则管理模块 | 加载业务规则库，维护规则编号、分类、适用表、严重等级 |
| 规则执行模块 | 按规则类型执行校验，生成标准异常结果 |
| AI 辅助分析模块 | 根据异常记录、规则描述和关联说明生成原因解释与修复建议 |
| 报告展示模块 | 输出异常清单、统计摘要、规则覆盖率和演示页面 |

### 5.2 数据流

```text
赛题5 Excel
  |
  v
数据导入模块
  |
  +--> 业务数据表集合
  +--> 规则库
  +--> 字段约束
  +--> 关联逻辑
  +--> 场景矩阵
  |
  v
规则执行模块
  |
  v
异常结果集合
  |
  +--> AI 辅助分析：异常原因、影响范围、修复建议
  |
  v
报告展示模块
```

### 5.3 规则执行策略

规则执行不要一开始追求“任意自然语言规则自动执行”，建议采用分层策略：

1. **第一层：结构化内置规则**
   - 对 R001 到 R030 中的规则做显式实现。
   - 优点是稳定、可控、易演示。

2. **第二层：规则模板化**
   - 将常见规则抽象成模板，例如非空、非负、枚举值、金额等式、跨表存在性、聚合一致性。
   - 后续新增规则时优先配置模板参数。

3. **第三层：AI 辅助转换**
   - AI 根据规则描述推荐规则模板和字段映射。
   - 生成结果需要人工确认或由测试样例自动验证，避免直接执行不可信代码。

首版建议完成第一层，并预留第二层的数据结构。

### 5.4 异常结果模型

异常结果建议统一为以下结构：

| 字段 | 说明 |
|---|---|
| `rule_id` | 规则编号，例如 R006 |
| `rule_name` | 规则名称 |
| `rule_type` | 单表校验、多表关联核对、指标一致性校验 |
| `severity` | 严重、警告 |
| `table_name` | 异常主表 |
| `record_key` | 异常记录主键，例如 ORD006 |
| `field_name` | 涉及字段，可为空 |
| `actual_value` | 实际值 |
| `expected_value` | 期望值或规则条件 |
| `reason` | 异常原因解释 |
| `suggestion` | 修复建议 |
| `scenario_id` | 对应场景编号，例如 S003 |

统一结果模型可以支撑页面展示、Markdown 报告、Excel 报告和后续接口输出。

## 6. 核心校验示例

首版演示建议重点覆盖以下规则，能够体现从简单到复杂的完整能力。

### 6.1 单表字段约束

示例规则：

- R001：金额非负校验
- R002：必填字段非空校验
- R003：金额类型校验

演示价值：

- 检出速度快。
- 结果直观。
- 适合作为工具入口演示。

### 6.2 单表业务规则

示例规则：

- R006：实付金额 = 订单金额 - 优惠金额，且实付金额 ≤ 订单金额。
- R007：上架商品售价不得低于成本价。

演示价值：

- 能体现业务约束，不只是字段校验。
- 可直接展示异常原因和修复建议。

### 6.3 多表关联核对

示例规则：

- 订单金额应等于订单明细小计金额之和。
- 明细商品 ID 必须存在于商品表。
- 支付表用户 ID 应与订单表用户 ID 一致。
- 出库数量应与订单明细数量一致。

演示价值：

- 最能体现赛题5的业务复杂度。
- 可以展示跨表链路定位能力。

### 6.4 指标一致性校验

示例规则：

- 已支付订单实付金额应等于支付成功金额之和。
- 库存变动后数量应等于变动前库存减去出库数量或加上入库数量。
- 退款金额不得大于支付金额。

演示价值：

- 适合做仪表盘统计。
- 能体现资金、库存等关键业务风险。

## 7. AI 增强点设计

AI 能力不建议放在基础校验是否正确上，而应放在“解释、归因、建议、辅助配置”上。

推荐三个增强点：

1. **异常原因解释**
   - 输入：规则描述、异常记录、关联逻辑。
   - 输出：面向测试人员的自然语言解释。
   - 示例：`ORD006 实付金额 520 大于订单金额 500，且不满足 订单金额 - 优惠金额 = 实付金额，可能导致支付对账异常。`

2. **修复建议生成**
   - 输入：异常类型、实际值、期望值、上下游表数据。
   - 输出：人工修复建议、核查步骤或修复 SQL 草案。
   - 注意：修复 SQL 只作为建议，不直接自动执行。

3. **规则模板推荐**
   - 输入：业务规则库中的规则描述和伪 SQL。
   - 输出：推荐规则类型、字段映射、关联表。
   - 用途：作为后续扩展亮点，证明工具可从规则文本半自动扩展。

## 8. 页面与报告设计

若做可视化界面，首版建议使用四个区域：

1. **数据导入区**
   - 上传或选择 Excel 文件。
   - 展示识别到的表、规则数、场景数。

2. **校验总览区**
   - 总规则数、已执行规则数、异常总数、严重异常数、警告数、耗时。
   - 按规则类型、业务表、严重等级展示分布。

3. **异常清单区**
   - 支持按规则、表、严重等级、场景筛选。
   - 点击异常可查看记录详情、关联数据和命中规则。

4. **原因与修复区**
   - 展示异常原因。
   - 展示影响范围。
   - 展示修复建议或核查步骤。

如果时间有限，也可以先输出 Markdown 或 Excel 报告，再补一个轻量 Web 页面用于演示。

## 9. 技术实现建议

考虑到赛题更重展示闭环，首版技术路线应简单可靠。

推荐方案：

| 层次 | 建议 |
|---|---|
| 数据读取 | Python + pandas/openpyxl 或 Java + Apache POI |
| 校验引擎 | 代码内置规则 + 规则模板 |
| 报告输出 | Markdown/Excel/HTML |
| 可视化 | Streamlit、Flask + 简单前端，或 Spring Boot + Vue |
| AI 接入 | 独立封装为可开关的分析服务 |

如果团队偏 Java，建议选 `Spring Boot + Apache POI + Vue/React`。如果团队追求快速演示，建议选 `Python + pandas + Streamlit`。

## 10. 实施优先级

建议按以下顺序推进：

1. **P0：离线核验闭环**
   - 读取 Excel。
   - 执行核心规则。
   - 输出异常清单和统计报告。

2. **P1：演示页面**
   - 展示数据导入、校验总览、异常列表、异常详情。
   - 支持按严重等级和规则类型筛选。

3. **P2：AI 解释与修复建议**
   - 根据规则和异常生成自然语言解释。
   - 生成核查步骤和修复建议。

4. **P3：规则模板化扩展**
   - 支持新增配置化规则。
   - AI 推荐规则模板和字段映射。

## 11. 风险与控制

| 风险 | 表现 | 控制方式 |
|---|---|---|
| 规则自动化过度承诺 | 自然语言规则无法稳定转成可执行逻辑 | 首版采用内置规则，AI 只做推荐和解释 |
| 跨表校验复杂 | 多表聚合、重复支付、库存流水容易算错 | 优先覆盖场景矩阵中的关键场景，逐条对齐预期结果 |
| 演示效果不足 | 只有命令行输出，难体现业务价值 | 至少提供汇总报告和异常详情页 |
| AI 生成内容不可信 | 修复建议可能不准确 | 明确标注为建议，保留原始规则和证据链 |
| 工具范围膨胀 | 想做成通用数据质量平台 | MVP 只围绕赛题5输入和 R001-R030 规则闭环 |

## 12. 推荐演示路径

比赛展示时建议按下面顺序演示：

1. 导入赛题5 Excel 文件。
2. 展示系统识别到 5 张业务表、30 条规则、15 个校验场景。
3. 点击开始校验。
4. 展示异常总览：严重异常、警告异常、按表分布、按规则类型分布。
5. 选择一个单表异常，例如 `ORD007 用户ID为空`。
6. 选择一个金额异常，例如 `ORD006 实付金额大于订单金额`。
7. 选择一个跨表异常，例如订单金额与明细金额不一致。
8. 选择一个库存异常，例如出库数量与订单明细数量不一致。
9. 展示 AI 生成的异常原因、影响范围和修复建议。
10. 导出核验报告。

## 13. 总结

赛题5具备最完整的输入资产和最清晰的业务闭环，适合作为主攻方向。首版不应追求“大而全”的数据质量平台，而应围绕赛题提供的 Excel、规则库和场景矩阵，做出一个稳定、可复现、解释性强的业务数据准确性验证工具。

最终交付重点应是：

1. 规则执行结果可信。
2. 异常定位链路清楚。
3. 业务原因解释准确。
4. 修复建议可操作。
5. 展示过程顺畅、可复现、可核验。
