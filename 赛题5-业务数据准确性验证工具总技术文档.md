# 赛题5：业务数据准确性验证工具总技术文档

## 1. 文档说明与当前实现状态

本文档整合《赛题5：业务数据准确性验证工具设计文档》和《赛题5 AI 规则模板通用推荐与执行器拓展设计》，并结合当前代码实现，作为比赛交付、演示说明和后续维护开发的总技术文档。

当前系统已经形成以下闭环：

1. 上传赛题 Excel，解析规则资产、场景矩阵和规则绑定。
2. 通过 Flyway seed 在 H2 中初始化赛题业务表。
3. 从数据库业务表读取标准化 `DataTable` / `DataRow`。
4. 按规则绑定选择模板执行器或内置规则执行逻辑。
5. 生成异常、证据、汇总统计和报告。
6. 提供前端工作台展示导入、校验、规则模板化推荐、异常详情和报告导出。
7. 提供 AI 辅助异常分析、只读 SQL 草案和规则模板推荐；AI 不可用或结果非法时自动降级到本地规则化结果。

当前实现以 MVP 演示和可复现核验为主。数据库表输入和 SQL 查询输入接口已预留，但当前代码返回 P3 预留提示；生产化业务库接入仍属于后续演进。

## 2. 总体目标与设计原则

系统目标是面向业务数据准确性验证场景，以可解释、可复现、可人工复核的方式发现业务数据疑点，输出规则、记录、字段、实际值、期望值和关联证据。

核心设计原则：

1. 规则判定确定性优先：AI 只做建议、解释和草案，不参与最终确定性判定。
2. 业务语义配置化：优先通过规则绑定和模板参数表达业务规则，内置规则作为复杂逻辑兜底。
3. 证据链可追溯：每条异常必须能追溯到规则、数据表、记录主键、字段值和关联证据。
4. 推荐需人工确认：AI 或本地推荐的模板绑定不会自动保存，必须由用户点击应用。
5. 输入源逐步数据库化：当前演示业务表来自 H2 seed，后续可替换为真实业务只读库。
6. 安全边界清晰：不自动执行修复 SQL，不保存生产明文凭证，不让 AI 输出绕过模板参数校验。
7. 前后端契约稳定：前端通过 REST API 消费数据，后端内部执行器演进不影响页面主要交互。

## 3. 系统架构与端到端流程

### 3.1 逻辑架构

```text
Vue 3 前端工作台
  |
  v
Spring Boot REST API
  |
  v
应用服务层
  ExcelImportService / ValidationService / RuleBindingService
  AiAssistService / ReportService
  |
  v
规则执行层
  TemplateRuleExecutor / 内置 R001-R030 规则逻辑
  RowExpressionEvaluator / TemplateExpressionEvaluator
  |
  v
数据访问层
  Spring Data JPA Repository / JdbcTemplate / Flyway
  |
  v
H2 演示系统库 + H2 演示业务表
```

### 3.2 端到端流程

```text
用户上传赛题 Excel
  |
  v
ExcelImportService 校验必需 sheet 并解析规则资产
  |
  v
BusinessTableDataProvider 从 H2 seed 业务表读取数据
  |
  v
保存 dataset、data_table_snapshot、rule_definition、rule_binding
  |
  v
用户查看规则模板化工作台，可请求 AI/本地模板推荐
  |
  v
用户启动校验
  |
  v
ValidationService 按规则绑定选择模板执行或内置执行
  |
  v
保存 validation_job、validation_finding、finding_evidence
  |
  v
前端查询汇总、异常列表和异常详情
  |
  v
用户可触发 AI 分析、SQL 草案、报告导出
```

## 4. 工程结构与技术栈

当前工程为前后端分离目录结构：

```text
competition/
  backend/
    pom.xml
    src/main/java/com/example/datavalidator/
      controller/
      service/
      domain/
      persistence/
      repository/
      config/
      exception/
      web/
    src/main/resources/
      application.yml
      application-h2.yml
      application-mysql.yml
      db/migration/
    src/test/java/com/example/datavalidator/service/
  frontend/
    package.json
    src/
      api/client.js
      App.vue
      main.js
      styles/app.css
  赛题5-*.md
```

技术栈如下：

| 层次 | 当前实现 |
|---|---|
| 后端框架 | Spring Boot 2.7.18 |
| Java 版本 | Java 11 |
| REST API | Spring MVC |
| 持久化 | Spring Data JPA |
| 数据库 | H2 演示库，预留 MySQL profile |
| 数据库迁移 | Flyway |
| Excel 解析 | Apache POI 5.2.5 |
| 数据库读取 | JdbcTemplate |
| JSON | Jackson `ObjectMapper` |
| 前端 | Vue 3 + Vite |
| AI 接入 | OpenAI-compatible chat completions 适配层 |
| 测试 | JUnit 5、Spring Boot Test、AssertJ |

当前前端未引入额外组件库，页面由 `App.vue` 和 `app.css` 直接实现。

## 5. 数据源与数据快照设计

### 5.1 当前数据源形态

当前阶段将赛题 Excel 中的业务 sheet 转换为 Flyway seed SQL，通过 `V3__seed_case5_business_tables.sql` 初始化以下业务表：

| 逻辑表 | 主键 | 职责 |
|---|---|---|
| `t_order` | `订单ID` | 订单主表 |
| `t_order_item` | `明细ID` | 订单明细 |
| `t_product` | `商品ID` | 商品信息 |
| `t_payment` | `支付ID` | 支付流水 |
| `t_inventory_log` | `流水ID` | 库存流水 |

`BusinessTableDataProvider` 使用 `JdbcTemplate` 从这些表读取数据，转换为统一领域模型：

1. `DataTable.logicalName` 保存逻辑表名。
2. `DataTable.sourceType` 标记为 `DATABASE_TABLE`。
3. `DataTable.headers` 来自固定表描述。
4. `DataRow.primaryKey` 来自主键字段。
5. BigDecimal、Timestamp、LocalDateTime 会转换为稳定字符串，便于规则执行和报告展示。

### 5.2 Excel 上传行为

当前 `ExcelImportService` 上传入口仍接收 `.xlsx`，但不再要求业务数据 sheet。必需 sheet 为：

1. `业务规则库`
2. `字段约束说明`
3. `关联逻辑说明`
4. `校验场景覆盖矩阵`
5. `使用说明`

上传后系统会：

1. 创建新的 `datasetId`。
2. 从 H2 业务表读取业务数据。
3. 从 Excel 解析规则定义和场景覆盖关系。
4. 保存数据集、业务表元数据快照、规则定义和默认规则绑定。
5. 不再将业务数据行写入 `data_row_snapshot`；业务行在校验时由 provider 从数据库读取。

### 5.3 预留数据库输入

`DataSourceController` 暴露 `/api/datasources/database-table` 与 `/api/datasources/sql-query`，当前返回“P3 预留能力”错误。后续生产化接入应拆分系统库和业务只读库，业务库连接只允许 `SELECT` 类读取。

## 6. 核心领域模型与持久化模型

### 6.1 领域模型

| 模型 | 职责 |
|---|---|
| `WorkbookDataset` | 一次可校验数据集，包含数据集标识、来源信息、业务表和规则集合 |
| `DataTable` | 标准化逻辑业务表，包含表名、表头、来源类型和数据行 |
| `DataRow` | 标准化数据行，包含行号、主键和字段值 map |
| `RuleDefinition` | 规则定义，包含规则编号、名称、分类、严重等级、描述、伪 SQL、适用表和场景 |
| `RuleBinding` | 规则执行绑定，指定 `BUILTIN` 或 `TEMPLATE`，以及模板编码和参数 |
| `ValidationFinding` | 规则命中的异常结果 |
| `Evidence` | 异常证据，记录关联表、记录、字段、计算过程和相关值 |

### 6.2 持久化表

当前 Flyway 初始化以下系统表：

| 表 | 职责 |
|---|---|
| `dataset` | 数据集来源、文件名、状态和导入时间 |
| `data_table_snapshot` | 数据集下业务表元数据、表头和行数 |
| `data_row_snapshot` | 兼容旧 Excel 行快照；当前业务表数据库化后不再写入业务行 |
| `rule_definition` | 数据集下规则定义 |
| `rule_binding` | 规则执行方式、模板编码和模板参数 JSON |
| `validation_job` | 校验任务状态、耗时和 AI 开关 |
| `validation_finding` | 异常结果主表 |
| `finding_evidence` | 异常证据明细 |
| `report_file` | 报告文件元数据 |

## 7. 规则执行体系

### 7.1 执行入口

`ValidationService.validate(datasetId, enableAiAnalysis)` 是当前校验入口。它负责：

1. 加载数据集和业务表。
2. 创建 `validation_job`。
3. 查询当前数据集的 `rule_binding`。
4. 遍历规则定义并执行。
5. 保存异常和证据。
6. 更新任务状态和耗时。

### 7.2 模板优先与内置兜底

规则执行策略为：

1. 若规则绑定存在且 `executorType=TEMPLATE`，调用 `TemplateRuleExecutor`。
2. 否则进入内置 R001 到 R030 规则分支。
3. 未识别规则不产生异常。

内置规则仍用于保证赛题 30 条规则的演示稳定性。模板执行用于通用化扩展和逐步降低规则编号特例。

### 7.3 异常结果

异常结果会保存：

1. 规则编号、规则名称、规则分类和严重等级。
2. 表名、记录主键和字段名。
3. 实际值、期望值和异常描述。
4. 原因、影响和建议。
5. 场景编号。
6. 证据明细，包括计算过程和关联值。

## 8. 规则模板与统一 DSL

### 8.1 当前模板清单

当前 `RuleTemplateController`、`TemplateRuleExecutor` 和 `TemplateBindingValidator` 支持以下 13 类模板：

| 模板 | 用途 | 关键参数 |
|---|---|---|
| `NOT_NULL` | 字段非空 | `tableName`, `fields` |
| `NON_NEGATIVE` | 数值非负 | `tableName`, `fields` |
| `NUMERIC_TYPE` | 数值类型 | `tableName`, `fields` |
| `FIELD_EXPRESSION` | 单表字段表达式 | `tableName`, `expression` |
| `ROW_EXPRESSION` | 结构化行表达式 | `tableName`, `conditions` |
| `EXISTS_IN_TABLE` | 简单跨表存在性 | `source`, `target`, `key` |
| `RELATION_EXISTS` | 结构化关系存在性 | `source`, `target`, `keys`, `expectExists` |
| `FIELD_EQUALS` | 旧版跨表字段一致 | `source`, `target`, `key`, `sourceField`, `targetField` |
| `JOIN_ASSERT` | 结构化跨表 join 断言 | `source`, `target`, `keys`, `assert` |
| `AGGREGATION_EQUALS` | 旧版聚合字段一致 | `source`, `target`, `groupBy`, `sum`, `targetField` |
| `AGGREGATE_ASSERT` | 结构化聚合断言 | `source`, `target`, `groupBy`, `aggregate`, `assert` |
| `DUPLICATE_ASSERT` | 结构化分组次数断言 | `table`, `groupBy`, `assert` |
| `DUPLICATE_CHECK` | 旧版重复记录检查 | `tableName`, `groupBy` |

### 8.2 一等结构化模板

后续规则推荐和默认绑定优先使用结构化模板：

1. 单行字段间关系优先使用 `ROW_EXPRESSION`。
2. 跨表存在或反存在优先使用 `RELATION_EXISTS`。
3. 跨表字段一致、表达式一致或禁止组合优先使用 `JOIN_ASSERT`。
4. 分组汇总、指标汇总和跨表聚合比较优先使用 `AGGREGATE_ASSERT`。
5. 唯一性、重复支付、最多/至少 N 次优先使用 `DUPLICATE_ASSERT`。

`FIELD_EQUALS`、`AGGREGATION_EQUALS`、`DUPLICATE_CHECK` 保留为历史兼容模板，不作为新增规则推荐的优先目标。

### 8.3 通用规则方法

赛题 30 条规则被抽象为以下通用方法，便于迁移到其他业务库：

| 通用方法 | 当前执行形态 | 覆盖场景 |
|---|---|---|
| `FIELD_CHECK` | `NOT_NULL`、`NON_NEGATIVE`、`NUMERIC_TYPE` | 必填、金额非负、数量非负、类型校验 |
| `ROW_ASSERT` | `ROW_EXPRESSION`、部分 `FIELD_EXPRESSION` | 金额关系、状态金额关系、时间逻辑、条件分支 |
| `RELATION_EXISTS` | `EXISTS_IN_TABLE`、`RELATION_EXISTS` | 主外键存在、条件存在、反向不存在 |
| `JOIN_ASSERT` | `JOIN_ASSERT`、兼容 `FIELD_EQUALS` | 用户一致、价格一致、下架商品不应出库 |
| `AGGREGATE_ASSERT` | `AGGREGATE_ASSERT`、兼容 `AGGREGATION_EQUALS` | 明细汇总、支付汇总、指标级聚合一致 |
| `DUPLICATE_ASSERT` | `DUPLICATE_ASSERT`、兼容 `DUPLICATE_CHECK` | 重复支付、唯一组合、次数上限 |

### 8.4 DSL 示例

`ROW_EXPRESSION` 可表达“实付金额 = 订单金额 - 优惠金额，且实付金额不大于订单金额”：

```json
{
  "tableName": "t_order",
  "conditions": [
    {
      "left": { "field": "实付金额" },
      "operator": "==",
      "right": {
        "op": "-",
        "left": { "field": "订单金额" },
        "right": { "field": "优惠金额" }
      }
    },
    {
      "left": { "field": "实付金额" },
      "operator": "<=",
      "right": { "field": "订单金额" }
    }
  ]
}
```

同一 DSL 可迁移到“应收金额 = 合同金额 - 减免金额”等非订单业务，只需替换表名和字段名。

## 9. AI 推荐、SQL 草案与异常分析

### 9.1 AI 能力边界

`AiAssistService` 当前提供三类能力：

1. 异常分析：根据异常和证据生成原因、影响、建议和证据摘要。
2. 只读 SQL 草案：生成辅助人工复核的 `SELECT` SQL，不自动执行。
3. 规则模板推荐：根据规则文本、伪 SQL 和字段快照推荐模板绑定。

所有 AI 输出都必须作为建议展示，不修改业务数据，不自动执行 SQL，不自动保存规则绑定。

### 9.2 推荐链路

规则模板推荐链路如下：

```text
规则定义 + 适用表 + 字段快照
  |
  v
RuleTemplateSemanticMapper 生成本地语义推荐
  |
  v
AiAssistService 调用 OpenAI-compatible 模型
  |
  v
解析模型 JSON 并统一执行模板参数校验
  |
  +-- 合法：返回 AI 推荐，generatedByAi=true
  |
  +-- 非法或模型不可用：返回本地推荐，generatedByAi=false，并附 warning
```

本地语义映射不仅是降级路径，也是安全基准。AI 推荐必须通过模板白名单、字段存在性、参数结构和可执行性校验后才能返回给前端。

### 9.3 SQL 草案安全

SQL 草案只允许只读核查用途。系统会拒绝危险意图和危险 SQL，例如多语句、写操作或结构变更语义。模型返回非法 SQL 时会降级到本地生成的只读 `SELECT` 草案。

## 10. 参数校验与安全边界

`TemplateBindingValidator` 是模板参数安全校验的核心。规则绑定更新和 AI 推荐结果会复用同一套校验口径，避免“推荐可展示但应用失败”或“应用成功但执行无效”。

通用校验包括：

1. 模板编码必须在支持清单内。
2. 表名必须存在于当前数据集字段快照。
3. 字段名必须存在于对应表头。
4. 参数结构必须符合模板要求。
5. 表达式、谓词、聚合、join key 和断言节点必须能被当前执行器解析。

专项校验包括：

1. `ROW_EXPRESSION` 校验结构化条件 AST。
2. `RELATION_EXISTS` 校验复合 key、sourceWhere、targetWhere、sourceExists。
3. `JOIN_ASSERT` 校验 join key、断言左右节点、操作符和容差。
4. `AGGREGATE_ASSERT` 校验分组、聚合函数、目标字段或目标聚合。
5. `DUPLICATE_ASSERT` 校验 groupBy、where 和 count 断言。

系统安全边界：

1. 不自动执行 AI 生成 SQL。
2. 不自动修改业务数据。
3. 不让 AI 推荐绕过模板参数校验。
4. 不在源码中硬编码真实密钥。
5. 当前业务数据来自演示 H2 seed，生产业务库接入需使用只读账号和独立数据源。

## 11. REST API 设计

通用响应结构为 `ApiResponse<T>`，成功时返回 `success=true` 和 `data`，失败时由全局异常处理返回错误信息。

### 11.1 文件与校验

| 方法 | 路径 | 职责 |
|---|---|---|
| `POST` | `/api/files/upload` | 上传赛题 Excel，创建数据集 |
| `POST` | `/api/validations` | 启动校验 |
| `GET` | `/api/validations/{jobId}/summary` | 查询校验汇总 |

`/api/validations` 请求体：

```json
{
  "datasetId": "ds_xxx",
  "enableAiAnalysis": false
}
```

### 11.2 异常查询

| 方法 | 路径 | 职责 |
|---|---|---|
| `GET` | `/api/findings` | 按 `jobId` 查询异常列表，可按 `severity`、`tableName`、`ruleId` 筛选 |
| `GET` | `/api/findings/{findingId}` | 查询异常详情和证据 |

### 11.3 规则与模板

| 方法 | 路径 | 职责 |
|---|---|---|
| `GET` | `/api/rules?datasetId=...` | 查询规则列表、执行形态、模板参数摘要 |
| `GET` | `/api/rules/{datasetId}/{ruleId}/binding` | 查询单条规则绑定 |
| `PUT` | `/api/rules/{datasetId}/{ruleId}/binding` | 更新规则绑定 |
| `GET` | `/api/rule-templates` | 查询支持的模板清单 |

规则绑定更新复用模板参数校验。切换回 `BUILTIN` 时保留原模板参数，便于后续切回模板执行。

### 11.4 AI 辅助

| 方法 | 路径 | 职责 |
|---|---|---|
| `GET` | `/api/ai/findings/{findingId}/analysis` | 生成或降级返回异常分析 |
| `POST` | `/api/ai/sql-drafts` | 生成只读 SQL 草案 |
| `POST` | `/api/ai/rule-binding/recommend` | 生成规则模板绑定推荐 |

模板推荐请求体：

```json
{
  "datasetId": "ds_xxx",
  "ruleId": "R006"
}
```

### 11.5 报告与预留数据源

| 方法 | 路径 | 职责 |
|---|---|---|
| `POST` | `/api/reports` | 生成报告 |
| `GET` | `/api/reports/{reportId}/download` | 下载报告 |
| `POST` | `/api/datasources/database-table` | P3 预留，当前返回提示 |
| `POST` | `/api/datasources/sql-query` | P3 预留，当前返回提示 |

## 12. 前端工作台设计

当前前端是 Vue 3 + Vite 单页工作台，主要文件为：

1. `frontend/src/App.vue`
2. `frontend/src/api/client.js`
3. `frontend/src/styles/app.css`

页面核心区域：

1. 顶部操作栏：选择 Excel、开始校验、导出报告。
2. 汇总指标区：业务表、规则数、异常总数、严重、警告、耗时。
3. 规则模板化工作台：
   - 规则检索。
   - 执行形态筛选。
   - 推荐状态筛选。
   - 当前绑定与推荐绑定对比。
   - 模板能力矩阵。
   - 推荐当前规则、批量推荐可见规则、应用推荐。
4. 异常列表与详情：
   - 支持严重等级、表、规则筛选。
   - 展示异常描述、实际值、期望值和建议。
   - 可触发 AI 分析和 SQL 草案。
5. 报告导出：生成报告后通过下载接口打开。

前端不直接构造模板合法性结论，模板是否可应用以后端校验结果为准。

## 13. 报告导出设计

`ReportService` 当前支持生成 Markdown 和 HTML 后缀报告：

1. `format` 为空时默认 `MARKDOWN`。
2. `format=HTML` 时生成 `.html`。
3. 其他格式当前按 Markdown 内容生成 `.md`。
4. 报告文件保存到 `app.storage.report-dir`，默认 `data/reports`。
5. `report_file` 记录 `reportId`、`jobId`、格式、路径和生成时间。

报告内容包括：

1. 任务编号。
2. 规则总数和已执行规则数。
3. 异常总数、严重异常、警告异常。
4. 校验耗时。
5. 异常明细表，包含规则、等级、表、主键、描述和建议。

Excel 报告属于原设计目标，当前代码未落地，后续可在 `ReportService` 中基于 Apache POI 扩展。

## 14. 配置、部署与演示流程

### 14.1 后端配置

默认配置：

```yaml
server:
  port: 8080

spring:
  profiles:
    active: h2

app:
  storage:
    upload-dir: data/uploads
    report-dir: data/reports
  validation:
    enable-ai-default: false
    stop-on-rule-error: false
  ai:
    enabled: ${LOCAL_AI_ENABLED:false}
    provider: openai-compatible
    endpoint: ${LOCAL_AI_ENDPOINT:}
    api-key: ${LOCAL_AI_API_KEY:}
    model: ${LOCAL_AI_MODEL:local-model}
```

H2 profile 使用：

1. `jdbc:h2:mem:data-validator;MODE=MySQL`
2. Flyway `classpath:db/migration`
3. JPA `ddl-auto=validate`
4. H2 console 路径 `/h2-console`

### 14.2 启动命令

后端：

```bash
cd backend
mvn spring-boot:run
```

前端：

```bash
cd frontend
npm install
npm run dev
```

前端默认 API 地址为 `http://localhost:8080`，可通过 `VITE_API_BASE` 覆盖。

### 14.3 演示流程

1. 启动后端，确认 Flyway 初始化完成。
2. 启动前端并打开 Vite 页面。
3. 上传 `各赛题输入案例/赛题5-业务数据准确性验证工具-输入案例.xlsx`。
4. 查看规则模板化工作台，选择规则并生成推荐。
5. 点击开始校验。
6. 查看汇总卡片、异常列表、异常详情和证据链。
7. 按需触发 AI 分析或只读 SQL 草案。
8. 点击导出报告并下载。

## 15. 测试策略与验收标准

### 15.1 后端测试

后端主验证命令：

```bash
cd backend
mvn test
```

当前测试覆盖包括：

| 测试类 | 覆盖内容 |
|---|---|
| `ExcelImportServiceTest` | Excel 不要求业务 sheet、不写业务行快照、默认绑定迁移 |
| `BusinessTableDataProviderTest` | H2 seed 业务表读取、表头、主键和行数 |
| `RuleTemplateSemanticMapperTest` | 本地语义映射、通用模板推荐、关系规则映射 |
| `TemplateRuleExecutorTest` | 13 类模板执行能力和异常详情 |
| `RuleBindingServiceTest` | 绑定更新、模板参数校验、执行形态切换 |
| `AiAssistServiceTest` | 异常分析、SQL 草案、AI 推荐、非法结果降级 |
| `OpenAiCompatibleChatClientTest` | OpenAI-compatible 请求和响应解析 |

### 15.2 前端验证

前端主验证命令：

```bash
cd frontend
npm run build
```

前端验收场景：

1. 上传后显示数据集摘要。
2. 规则模板化工作台能展示规则、当前绑定、推荐绑定和 warning。
3. 推荐当前规则和批量推荐可见规则可调用后端接口。
4. 应用推荐后规则列表刷新。
5. 开始校验后展示汇总指标。
6. 异常列表筛选和详情展示正常。
7. AI 分析和 SQL 草案只展示建议，不自动执行。
8. 报告导出可生成并下载文件。

### 15.3 文档一致性验收

本总文档应满足：

1. 不描述当前未落地的前端组件库依赖。
2. 模板清单与 `RuleTemplateController`、`TemplateRuleExecutor`、`TemplateBindingValidator` 保持一致。
3. API 清单与 controller 实现保持一致。
4. 测试章节引用当前存在的测试类和命令。
5. 明确区分当前已实现能力和后续演进能力。

## 16. 当前边界与后续演进

### 16.1 当前边界

1. 当前演示业务表来自 H2 seed，不是动态生产业务库接入。
2. `/api/datasources/database-table` 和 `/api/datasources/sql-query` 仍是预留接口。
3. 规则执行仍保留 R001 到 R030 内置兜底逻辑。
4. Excel 报告未落地，当前报告输出主要是 Markdown 和 HTML。
5. 未建设权限系统、审计系统、多租户、任务调度和大规模异步执行。
6. AI 推荐和 SQL 草案只作为人工参考，不作为自动执行逻辑。

### 16.2 后续演进

1. 拆分系统库和业务只读库，新增业务库连接配置、连接测试和只读权限校验。
2. 将更多默认绑定迁移到结构化模板，减少规则编号特例。
3. 将 `FIELD_EQUALS`、`AGGREGATION_EQUALS`、`DUPLICATE_CHECK` 降级为历史兼容入口。
4. 扩展 `ROW_EXPRESSION`、`RELATION_EXISTS`、`JOIN_ASSERT`、`AGGREGATE_ASSERT` 的表达能力和错误提示。
5. 报告导出增加 Excel 格式，按汇总、异常明细、规则覆盖、证据链分 sheet 输出。
6. 前端增强模板参数结构化展示、当前绑定与推荐绑定差异高亮、批量应用前确认。
7. 增加真实业务库只读输入的集成测试和安全测试。
8. 增加规则版本、绑定版本和报告可复现元数据。

## 总结

当前系统已经从“固定 Excel 离线核验”演进为“规则资产来自 Excel、业务数据来自演示数据库、规则执行可模板化、AI 推荐可降级”的可演示工具。后续重点不是扩大 AI 自动执行范围，而是继续强化结构化 DSL、只读业务库接入、模板参数校验和证据链展示，让新增业务规则尽量通过模板参数配置完成。
