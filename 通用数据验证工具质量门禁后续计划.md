# 通用数据验证工具质量门禁后续计划

## 1. 定位

质量门禁能力作为通用数据验证工具的后续专项规划，不阻塞当前“先让工具好用”的主线。

当前主线优先解决：

1. JDBC 快速上手。
2. lint 修复建议。
3. 规则片段库。
4. AI 推荐补充。

质量门禁专项后续再解决：

1. 单次校验结果是否能进入流水线。
2. 校验失败原因是否可追溯。
3. 规则资产覆盖是否可量化。
4. CI、脚本和外部系统是否能稳定消费工具输出。

## 2. 目标效果

质量门禁专项完成后，工具需要能回答以下问题：

1. 这批数据是否可以进入下一环节。
2. 本次校验执行了多少规则，覆盖了多少表和字段。
3. 命中了多少严重异常和警告异常。
4. 本次结果对应哪个规则包、哪个数据源摘要和哪些报告文件。
5. CI 或脚本能否只依赖退出码和机器可读摘要做阻断。

## 3. 计划能力

### 3.1 Gate Summary

新增轻量门禁摘要输出，作为 JSON 报告之外的流水线入口。

建议命令形态：

```bash
data-validator run --config validator.yml --gate-summary-output gate-summary.json
```

摘要字段建议包含：

1. `gateStatus`：`PASSED` 或 `FAILED`。
2. `exitCode`：沿用当前 CLI 退出码。
3. `totalRules`、`executedRules`。
4. `findingCount`、`criticalCount`、`warningCount`。
5. `sourceSummary`：数据源类型、表数量、总行数、表级字段数和行数。
6. `rulePackageHash`：规则包 hash。
7. `reports`：JSON、Markdown、JUnit XML 等报告路径。

### 3.2 规则覆盖摘要

在门禁摘要或独立报告中补充规则资产覆盖信息。

第一版建议只做轻量统计：

1. 表覆盖：规则引用了哪些表，哪些表未被规则引用。
2. 字段覆盖：规则引用了哪些字段，哪些字段未被规则引用。
3. 模板分布：各 `templateCode` 使用数量。
4. 等级分布：`CRITICAL` 与 `WARNING` 规则数量。

暂不做复杂覆盖率阈值和趋势分析。

### 3.3 CI 集成模板

发布包或 README 中补充可复制的 CI 片段。

第一版只写文档模板，不新增真实仓库 workflow。

建议覆盖：

1. GitHub Actions shell 示例。
2. Jenkins shell 示例。
3. 只依赖退出码的最小门禁示例。
4. 读取 `gate-summary.json` 的机器可读示例。

### 3.4 订单履约 Gate Demo

基于后续易用性阶段的订单履约样板，整理一个门禁演示版本。

建议策略：

1. 使用通用 YAML 规则包和模板执行器。
2. 不新增 Java 内置规则 switch。
3. 提炼 6 到 8 条代表规则，覆盖必填、金额关系、跨表存在、库存连续性和支付异常。
4. 样例预期产生严重异常，`run` 返回退出码 `2`。
5. README 明确说明：退出码 `2` 表示命中质量门禁，不表示工具执行失败。

## 4. 暂不做事项

1. 不做完整历史趋势看板。
2. 不做多次基线对比。
3. 不接入完整 Web 工作台。
4. 不新增真实仓库 CI workflow。
5. 不把 AI 结果作为门禁判定依据。
6. 不自动修改业务数据。

## 5. 建议启动条件

满足以下条件后，再启动质量门禁专项：

1. `init jdbc --template order-fulfillment` 已能生成可运行样板。
2. lint 高频错误已有可读修复建议。
3. 规则片段库已覆盖主要模板类型。
4. 发布包或脚本能稳定执行 `lint`、`run` 和 `recommend`。

## 6. 验收口径

1. CI 可通过退出码判断门禁是否失败。
2. CI 可读取 `gate-summary.json` 获取规则数、异常数、数据摘要和报告路径。
3. JUnit XML 可继续用于测试平台展示异常。
4. 订单履约 gate demo 可稳定演示一次失败门禁。
5. 所有门禁输出不包含明文密码、token、secret 或本机绝对路径。
