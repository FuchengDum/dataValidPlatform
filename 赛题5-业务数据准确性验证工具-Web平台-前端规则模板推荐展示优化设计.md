# 赛题5：业务数据准确性验证工具-Web平台-前端规则模板推荐展示优化设计

## 设计目标

规则模板化通用化是本工具的核心演示能力。本次前端优化将“规则选择、模板绑定、AI 推荐、模板覆盖”从侧栏小卡片提升为第一屏工作台，让评审能直接看到规则从内置执行到通用模板执行的迁移过程。

本阶段不调整后端接口，不引入新的前端依赖，继续基于 Vue 3 + Vite 单页应用实现。

## 页面布局

页面保持“导入、校验、报告导出”的顶部主流程不变，在指标区下方新增“规则模板化工作台”。

工作台采用三栏布局：

1. 左侧规则选择区
   - 展示规则编号、规则名称、当前执行形态、模板编码、推荐状态。
   - 支持按规则关键字、执行形态、推荐状态筛选。
   - 点击规则后，中间区展示该规则的当前绑定与推荐绑定。

2. 中间绑定对比区
   - 左侧展示当前绑定：执行器类型、模板编码、模板参数。
   - 右侧展示 AI 推荐绑定：推荐模板编码、参数、来源、模型生成或本地推荐状态。
   - 提供“切到模板 / 切回内置”“应用推荐”两个主要操作。

3. 右侧模板能力矩阵
   - 展示 `ROW_EXPRESSION`、`RELATION_EXISTS`、`JOIN_ASSERT`、`AGGREGATE_ASSERT`、`DUPLICATE_ASSERT` 的规则覆盖数量。
   - 展示模板执行、内置执行、推荐失败数量。
   - 用进度条强化模板覆盖度，便于演示通用模板的覆盖范围。

异常疑点清单保留在工作台下方，异常筛选迁移到清单顶部，避免和规则模板能力混在同一个侧栏。

## 交互流程

1. 上传 Excel 后，前端通过现有 `/api/rules` 获取规则清单，并默认选中第一条规则。
2. 点击“推荐当前规则”，调用 `/api/ai/rule-binding/recommend`，结果写入当前规则推荐状态。
3. 点击“批量推荐可见规则”，按当前筛选后的规则列表逐条调用单条推荐接口，并记录成功、失败、模型生成、本地推荐状态。
4. 点击“应用推荐”，调用 `/api/rules/{datasetId}/{ruleId}/binding`，将该规则切换为模板执行。
5. 点击“开始校验”后，异常清单和详情面板沿用原流程。

## 状态设计

前端新增本地状态：

- `selectedRuleId`：当前选中的规则编号。
- `ruleFilters`：规则关键字、执行形态、推荐状态筛选条件。
- `recommendationErrors`：按规则编号记录推荐失败原因。
- `recommendationBatchProgress`：批量推荐的总数与已完成数。
- `recommendationLoading`：规则推荐相关操作的加载状态。

前端新增派生数据：

- `filteredRules`：按规则筛选条件过滤后的规则列表。
- `selectedRule`：当前选中规则。
- `selectedRecommendation`：当前选中规则的推荐结果。
- `templateCoverageStats`：模板覆盖、模板执行、内置执行和各模板类型覆盖数量。
- `recommendationStats`：已推荐、模型生成、本地推荐、推荐失败数量。

## 接口复用

本次设计复用现有接口：

- `GET /api/rules?datasetId=...`
- `POST /api/ai/rule-binding/recommend`
- `PUT /api/rules/{datasetId}/{ruleId}/binding`

批量推荐先在前端串行调用单条推荐接口，不新增后端 batch endpoint。这样可以保持后端契约稳定，同时满足演示时快速生成多条推荐状态的需要。

## 响应式设计

桌面端使用三栏工作台，突出规则选择、绑定对比、能力矩阵的并列关系。

小屏端改为单列堆叠，顺序为规则选择、绑定对比、模板能力矩阵、异常清单、异常详情。所有按钮和筛选项保持固定高度，文本允许换行，避免内容溢出或遮挡。

## 验收标准

- 未上传数据时，模板工作台显示空态，推荐与应用按钮禁用合理。
- 上传 Excel 后，规则列表可展示并默认选中第一条规则。
- 单条推荐结果可出现在绑定对比区，并展示模板编码、参数、来源和告警。
- 批量推荐可见规则时，进度和失败状态可见，单条失败不阻断后续规则。
- 应用推荐后，规则绑定刷新为模板执行。
- 异常校验、异常详情、AI 分析、SQL 草案和报告导出流程不回归。
- `npm run build` 构建通过。
