# 截图索引与证明点说明

## 1. 补录规则

截图必须来源于真实运行界面或真实命令执行结果。本轮截图已存放在同目录
`screenshots/` 下，文件名采用 `S01.png` 至 `S14.png`；截图证明范围以
下表及自动验证证据摘录为准。

## 2. Web 截图清单

| 编号 | 截图内容 | 证明点 | 文件/状态 |
|---|---|---|---|
| S01 | Web 工作台初始入口 | 提供统一核查入口和规则工作台界面 | `screenshots/S01.png`，已采集 |
| S02 | 校验完成后的汇总指标与推荐绑定 | 展示 5 表、30 规则、59 异常及模板推荐界面 | `screenshots/S02.png`，已采集 |
| S03 | `R020` 支付金额核对详情 | 跨表对账与证据链能力 | `screenshots/S03.png`，已采集 |
| S04 | `R029` 重复支付详情 | 资金风险识别能力与模型分析展示 | `screenshots/S04.png`，已采集 |
| S05 | `R020` AI 分析模型生成结果 | 真实模型辅助解释能力，不替代规则判定 | `screenshots/S05.png`，已采集（模型生成） |
| S06 | AI 本地降级分析结果 | 模型不可用或未采用时的稳定辅助路径 | `screenshots/S06.png`，已采集（本地降级） |
| S07 | 只读 SQL 草案 | 人工核查辅助，不自动执行 | `screenshots/S07.png`，已采集 |
| S08 | Markdown 报告导出结果 | 结果可留存、可提交，统计与 Web 当前基线一致 | `screenshots/S08.png`，已替换并核对 |

## 3. CLI 与工程截图清单

| 编号 | 截图内容 | 证明点 | 文件/状态 |
|---|---|---|---|
| S09 | JDBC 样例 `lint` 输出 | 新数据源和规则配置可检查 | `screenshots/S09.png`，已采集 |
| S10 | JDBC 样例 `run --json --no-report` 输出 | 通用校验与机器可读结果 | `screenshots/S10.png`，已采集 |
| S11 | `recommend` 输出及候选文件内容 | AI/本地推荐辅助规则扩展，候选需人工审阅 | `screenshots/S11.png`，已采集 |
| S12 | Case5 CLI 固定复现输出 | 可复现的赛题验收证据 | `screenshots/S12.png`，已采集 |
| S13 | `mvn test` 结果 | 后端回归验证 | `screenshots/S13.png`，已采集 |
| S14 | `npm run build` 结果 | 前端可构建验证 | `screenshots/S14.png`，已采集 |

上述终端截图与同目录 `04-自动验证证据摘录.md` 中的文本证据共同使用，
固定统计以文本摘录和当前提交口径为准。

## 4. 安全证据页素材

安全证据页至少组合以下信息：

| 证据 | 说明 | 状态 |
|---|---|---|
| SQL 草案及“仅用于人工核查”提示 | 系统只生成核查草案，不自动执行 | `S07` 已采集 |
| 模型生成与本地降级来源标识 | AI 为辅助能力，降级不影响规则结果 | `S05`、`S06` 已采集 |
| JDBC 样例与只读账号说明 | 接入业务库遵循只读边界 | `S09`、`S10` 及文档依据 |
| `recommend` 候选路径 | 不自动覆盖正式规则资产 | `S11` 已采集 |

## 5. 当前已有文件型证据

以下附件已存在于仓库，可直接作为截图之外的佐证：

- `examples/case5-seed/expected-result.md`
- `examples/case5-seed/reports/validation-report-20260523161645.md`
- `examples/case5-seed/reports/validation-report-20260523161645.json`
- `examples/generic-jdbc/README.md`
- `通用数据验证工具-CLI-规则片段库.md`
- `通用数据验证工具-CLI-AI推荐补充入口.md`

## 6. 证据使用边界

- `S01` 是工作台初始入口截图，不作为规则与数据已加载的证明；加载和校验
  汇总以 `S02` 及自动验证摘录为准。
- `S05` 为真实模型生成的辅助分析展示，只证明解释能力；异常判定仍由
  确定性规则完成。
- `S08` 已更换为与当前 Web 核验基线一致的报告截图：30 条规则、59 条
  异常、严重 52、警告 7。
- 易用性试用记录仍需真实用户填写，当前截图和自动验证证据不替代试用结论。
